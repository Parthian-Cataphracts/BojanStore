# وضعیت پروژه

فهرست کارهای باقی‌مانده پروژه. هر آیتم که انجام شد، تیک بخورد.

## در انتظار انجام

- [ ] مشکل نمایش عکس‌ها همراه با متن‌ها به‌طور کامل بررسی و ریسپانسیو شوند (صفحه به صفحه — فقط لوگو و ساختار کلی در این دور بررسی شد)
- [ ] کش (cache) به پروژه اضافه شود
- [ ] Celery اضافه شود (نکته: بک‌اند دات‌نت است؛ معادل آن Hangfire/Quartz.NET خواهد بود مگر اینکه یک سرویس صف جدا با پایتون مدنظر باشد — نیاز به تصمیم دارد)
- [ ] لینک صفحات جزئیات (detail) به‌جای ID از slug استفاده کنند

## انجام‌شده

- [x] وضعیت سرور در پنل ادمین (داشبورد): آپ‌تایم، CPU، حافظه، دیسک، سلامت پایگاه‌داده — بک‌اند دات‌نت ۱۰ (`GET /api/admin/system/status`) + کارت `ServerStatusCard` در داشبورد
- [x] حالت تعمیر و نگهداری: سوییچ پنل ادمین حالا واقعاً فروشگاه را می‌بندد (middleware فروشگاه + صفحه `/maintenance` + endpoint عمومی `/api/store/status`)؛ پیش‌نمایش با `?maintenance_bypass=<MAINTENANCE_BYPASS_SECRET>`
- [x] لایو چت: ویجت شناور در فروشگاه (بدون نیاز به ورود) + صفحه «گفتگوی آنلاین» در پنل ادمین زیر پشتیبانی — پیام‌رسانی واقعی روی دیتابیس، به‌روزرسانی هر ۴ ثانیه (polling، نه وب‌سوکت)
- [x] لوگو: کامپوننت `BrandLogo` اضافه شد — کافی‌ست فایل را در `frontend/apps/storefront/public/logo.svg` و `frontend/apps/admin/public/logo.svg` بگذارید (svg یا png)، بدون نیاز به تغییر کد جایگزین می‌شود؛ تا آن زمان همان وردمارک متنی «بوژان» نمایش داده می‌شود
- [x] منوهای پنل ادمین کشویی شدند — با کلیک روی عنوان هر گروه بسته/باز می‌شود، وضعیت در مرورگر ذخیره می‌ماند، و گروه حاوی صفحه فعلی همیشه باز است
