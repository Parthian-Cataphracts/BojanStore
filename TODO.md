# وضعیت پروژه

فهرست کارهای باقی‌مانده پروژه. هر آیتم که انجام شد، تیک بخورد.

بعد از یک ممیزی کامل (معماری، بک‌اند/دیتابیس/API، امنیت، UI/UX و دسترس‌پذیری،
سئو، کارایی، تست) فهرست زیر بازنویسی شد. چند مورد که قبلاً «انجام‌نشده» ثبت شده
بود در واقع ساخته شده ولی به بقیه‌ی سیستم وصل نشده بود، و چند ایراد جدی هم پیدا
شد که اصلاً در فهرست نبود.

## بلاک‌کننده‌ی انتشار (تا وقتی این‌ها باز است، نباید روی سرور اصلی رفت)

1. [x] **وضعیت پرداخت سفارش** — انجام شد. `Order.PaymentStatus` اضافه شد و عمداً
   از وضعیت ارسال جداست (یکی مسیر پول است، یکی مسیر کالا). `MarkPaid`،
   `MarkPaymentFailed` و `MarkRefunded` هر سه idempotent‌اند، اپراتور با
   `POST /admin/orders/payment` پرداخت را ثبت می‌کند (فقط owner، داخل تراکنش و
   زیر قفل ردیف سفارش)، و هیچ سفارشی جز «پرداخت در محل» بدون پرداخت به
   «ارسال‌شده» نمی‌رود. مهاجرت، ردیف‌های قدیمی را هم پر می‌کند.
   **خودِ درگاه هنوز وصل نیست** — آن کار جداست و پایین در فهرست آمده.
2. [x] **بکاپ‌گیری** — انجام شد. `QueueBackupAsync` فقط job را در صف می‌گذارد و
   `BackupWorker` کار را انجام می‌دهد: یک zip شامل دامپ واقعی دیتابیس با
   `pg_dump` (فرمت custom، پس `pg_restore` می‌تواند فقط یک جدول را برگرداند) و
   آرشیو کل درخت آپلودها. رمز دیتابیس از طریق environment به فرزند داده می‌شود نه
   روی خط فرمان. شکست، شکست ثبت می‌شود: نبود ابزار، دامپ نیمه‌کاره یا نوع ناشناخته
   هرکدام دلیل را ذخیره می‌کنند، فایل نیم‌نوشته پاک می‌شود و چیزی برای دانلود
   نمی‌ماند. ایمیج runtime حالا `postgresql-client` دارد.
3. [x] **نیمه‌ی اپراتور مرجوعی‌ها** — بک‌اند و API انجام شد. صف
   `GET /admin/returns`، جزئیات `GET /admin/returns/{id}` و تصمیم
   `POST /admin/returns/decide`. مبلغ بازگشتی از روی قیمت‌های ثبت‌شده‌ی خود سفارش
   حساب می‌شود (`ReturnRefund`): سهم تخفیف سفارش به نسبت کسر می‌شود، هزینه‌ی ارسال
   هرگز برنمی‌گردد، و **هیچ عددی از بدنه‌ی درخواست گرفته نمی‌شود**. مرجوعی روی
   سفارشی که پولش هرگز وصول نشده رد می‌شود. برگرداندن کالا به انبار قضاوت اپراتور
   است نه نتیجه‌ی خودکارِ «دریافت شد».
   **صفحه‌ی پنل هنوز ساخته نشده** — پایین در فهرست آمده.
4. [x] **نوتیفیکیشن** — انجام شد. `NotificationWorker` صف را می‌خواند و
   `DispatchAsync` را صدا می‌زند، پس کمپین زمان‌بندی‌شده واقعاً اجرا می‌شود و
   ارسال انبوه قابل ازسرگیری است.
5. [x] **پنل پیامک** — انجام شد. سرویس **SMS.ir** پیاده‌سازی شد و از پنل
   (تنظیمات ← پیامک) تنظیم می‌شود: کلید وب‌سرویس رمزنگاری‌شده ذخیره می‌شود و
   هرگز به مرورگر برنمی‌گردد، و دکمه‌ی «ارسال پیامک آزمایشی» یک پیامک واقعی
   می‌فرستد و پاسخ سرویس را عیناً نشان می‌دهد. **پورت `ISmsSender` به دو متد
   شکست**، چون این دو واقعاً دو محصول جدا هستند: کد ورود از **خط خدماتی** و روی
   **قالب تأییدشده** می‌رود (تنها راهی که به شماره‌های مسدودکننده‌ی تبلیغات هم
   می‌رسد) و کمپین از **خط تبلیغاتی** با متن آزاد. یکی کردنشان یعنی یک ادغام که
   کامل به‌نظر می‌رسد و دقیقاً برای همان مشتری‌هایی شکست می‌خورد که بیشتر لازمش
   دارند. گیت استارت‌آپ برداشته شد: وقتی حساب از خود پنل وارد می‌شود، برنامه‌ای
   که بالا نمی‌آید یعنی مالک هرگز به صفحه‌ی تنظیمش نمی‌رسد — به‌جایش کد ورودِ
   ارسال‌نشده با سطح error لاگ می‌شود، بدون خود کد.
6. [x] **ایمیل‌ها** — انجام شد. `SmtpEmailSender` روی MailKit با **همان حساب
   صندوق پستی پشتیبانی** کار می‌کند: یک ست اعتبارنامه که یک‌بار وارد می‌شود، نه دو
   تنظیم جدا که باید هماهنگ بمانند. تنظیمات per-message خوانده می‌شود تا تغییر
   حساب از پنل بدون deploy اثر کند، و وقتی صندوق خاموش یا نیمه‌تنظیم است به
   `ConsoleEmailSender` برمی‌گردد (فروشگاهی که هنوز راه‌اندازی نشده باید بتواند
   سفارش بگیرد). شکست ارسال لاگ می‌شود نه throw — هر فراخوان بعد از commit شدن
   کار اصلی می‌رسد، پس throw یعنی گفتن «سفارشت ثبت نشد» به مشتری در حالی که شده.

## در انتظار انجام

ترتیب فعلاً همینه که ثبت شده؛ بعداً ترتیب اولویت اصلاح می‌شود.

1. [x] پیگیری سفارش — از قبل کامل بود: `GET /orders/track` (تطبیق هم‌زمان شماره
   سفارش و موبایل، با rate limit سخت)، صفحه‌ی ۳۰ و `TrackOrderForm`. این آیتم
   کهنه در فهرست مانده بود.
2. [x] بکاپ‌گیری — بالا توضیح داده شد
3. [x] درگاه پرداخت — انجام شد. **زرین‌پال** روی API نسخه‌ی ۴ پیاده شد و از پنل
   (تنظیمات ← پرداخت) تنظیم می‌شود: انتخاب درگاه، شناسه‌ی پذیرنده (رمزنگاری‌شده،
   هرگز برنگشتنی)، سوییچ sandbox و آدرس بازگشت. **انتخاب درگاه از registration به
   یک ردیف دیتابیس منتقل شد**، چون مالک آن را روی فروشگاهِ در حال کار وارد می‌کند و
   باید روی سفارش بعدی اثر کند نه روی deploy بعدی. مبلغ‌ها با `IRT` می‌روند (همه‌جای
   این پروژه تومان است) و کد ۱۰۱ دقیقاً مثل ۱۰۰ «پرداخت‌شده» خوانده می‌شود.
   **حلقه‌ی پرداخت بسته شد**: `POST /me/payments/callback` مرجع بازگشتی را به
   سفارش یا شارژ کیف پول نگاشت می‌کند، مبلغ را از خود سفارش برمی‌دارد نه از
   درخواست، و زیر قفل ردیف سفارش idempotent است. `Status=OK` مرورگر هیچ‌جا خوانده
   نمی‌شود. **`PaymentReconciliationWorker`** هم سفارش‌هایی را که پول‌شان گرفته شده
   ولی خریدار هرگز برنگشته پیدا و تسویه می‌کند.
4. [x] پنل پیامک — بالا توضیح داده شد
5. [x] **صفحه‌ی مرجوعی‌ها در پنل ادمین** — انجام شد. صف (`/returns`) و صفحه‌ی
   جزئیات (`/returns/[id]`) کنار سفارش‌ها و زیر همان section permission. مبلغ
   بازگشتی و پرچم `payable` هم در جدول صف و هم قبل از کلیک تصمیم نشان داده
   می‌شوند، انتخاب «برگشت به انبار» فقط در مرحله‌ی دریافت پرسیده می‌شود، و
   دکمه‌ها فقط وضعیت‌هایی را پیشنهاد می‌دهند که بک‌اند می‌پذیرد (رو به جلو).
   شرح اصلی برای مرجع: بک‌اند و API کامل ساخته شده‌اند
   (`GET /admin/returns`، `GET /admin/returns/{id}`، `POST /admin/returns/decide`)
   و `return-decision` هم در `resources.ts` اعلام شده، ولی **هیچ صفحه‌ای در پنل
   برای مدیریت مرجوعی‌ها وجود ندارد**. یعنی اپراتور فعلاً هیچ راهی برای دیدن یا
   تصمیم‌گیری روی مرجوعی‌ها از طریق رابط کاربری ندارد. صفحه باید صف را نشان دهد،
   مبلغ تخمینی بازگشت (`refundEstimate`) و پرچم `payable` را قبل از کلیک نمایش
   دهد، و انتخاب «برگشت به انبار» را در مرحله‌ی دریافت از اپراتور بگیرد.
6. [x] **باگ صفحه‌بندی صف شارژ کیف پول** — رفع شد؛ ترتیب به
   `(items, total, normalised.Page, normalised.PageSize)` برگشت و بقیه‌ی
   ۲۷ فراخوانی `new Paged<...>` هم بررسی شد (فقط همین یکی غلط بود). شرحِ اصلیِ
   باگ برای مرجع: در `ListWalletTopUpsAsync`
   (`AdminQueries.cs`) آرگومان‌های `Paged<T>` جابه‌جا پاس داده شده‌اند:
   `Paged<T>(Items, Total, Page, PageSize)` است ولی فراخوانی
   `(items, normalised.Page, normalised.PageSize, total)` می‌فرستد — پس `Total`
   شماره‌ی صفحه را می‌گیرد و `PageSize` تعداد کل ردیف‌ها را. یعنی
   `GET /api/admin/wallet/topups` صفحه‌بندی غلط برمی‌گرداند. اصلاح: ترتیب به
   `(items, total, normalised.Page, normalised.PageSize)` برگردد، و بقیه‌ی
   `new Paged<...>`های پروژه هم برای همین اشتباه بررسی شوند.
7. [x] **چت** — رفع شد. شناسه‌ی visitor دیگر در `localStorage` نیست و از URL هم
   گرفته نمی‌شود: سرور آن را صادر می‌کند و در کوکی امضاشده‌ی `httpOnly` نگه می‌دارد،
   پس نه اسکریپت صفحه می‌تواند بخواندش و نه کسی که یک GUID را جایی دیده می‌تواند
   بازپخشش کند. خواندن گفتگو هم مثل نوشتن rate limit گرفت — بدون سقف، آن endpoint
   یک oracle رایگان روی گفتگوهای پشتیبانی بود.
8. [x] advisoryهای وابستگی — رفع شد. هر دو از دل `next` می‌آمدند که نسخه‌ی
   قدیمی‌تری از آنچه این workspace مستقیم دارد را bundle می‌کند. `overrides` هم در
   `package.json` (برای pnpm 9 که `packageManager` پین کرده) و هم در
   `pnpm-workspace.yaml` (برای pnpm 10) اعلام شد، چون هرکدام دیگری را نادیده
   می‌گیرد. `pnpm audit --prod`: صفر.
9. [x] قرارداد خطا در proxyهای فرانت — رفع شد. هر دو app حالا کلید ماشینیِ
    RFC 7807 را ترجمه می‌کنند (`lib/api/problem.ts`)، و جایی که بک‌اند با `detail`
    دقیق‌ترش می‌کند همان جفت را می‌خوانند: «کد تخفیف ناشناخته» با «قبلاً استفاده
    شده» یکی نیست، و «این بخش برای نقش شما فعال نشده» می‌گوید از کجا فعال می‌شود.
10. [x] سقف کش و rate limit کاتالوگ — رفع شد. `SizeLimit` روی `IMemoryCache` (هر
    entry اندازه‌ی ۱، پس سقف یک شمارش است نه یک تخمین بایت)، miss فقط ۳۰ ثانیه
    نگه داشته می‌شود نه ۵ دقیقه — نه انباشته می‌شود و نه راه‌رفتن روی slugها به
    دیتابیس می‌رسد — و کل گروه خواندن‌های عمومی کاتالوگ rate limit گرفت.
11. [x] جست‌وجوی محصولات — رفع شد. چهار شرط `LIKE '%…%'` سر جایشان ماندند ولی
    حالا ایندکس trigram (`pg_trgm` + GIN) دارند، که تنها چیزی است که یک الگوی با
    wildcard ابتدایی را قابل ایندکس می‌کند و روی متن فارسی هم کار می‌کند. فیلتر
    دسته‌بندی و برند هم از سه رفت‌وبرگشت به یکی رسید.
12. [x] فراخوانی درگاه از داخل تراکنش checkout بیرون آمد. سفارش اول commit
    می‌شود و بعد session پرداخت گرفته می‌شود، پس یک درگاهِ کند دیگر قفل ردیف
    محصولات را نگه نمی‌دارد. اگر درگاه در دسترس نباشد پاسخ `payment-unavailable`
    به‌همراه شماره سفارش است، نه یک خطای عمومی: سفارش واقعی است و مشتری باید به
    آن برگردد نه اینکه سفارش دوم ثبت کند.
13. [~] CI اضافه شد (`.github/workflows/ci.yml`): تست بک‌اند، تایپ‌چک/تست/بیلد
    فرانت، `pnpm audit --prod`، و — مهم‌تر از همه — replay کردن **همه‌ی مهاجرت‌ها
    روی یک PostgreSQL واقعی** از صفر، بار دوم برای اثبات no-op بودن، و بررسی
    اینکه مدل چیزی migrate-نشده ندارد. **باز مانده:** خودِ suite هنوز روی SQLite
    اجرا می‌شود، پس قفل‌های `FOR UPDATE` هنوز در تست اجرا نمی‌شوند — برای آن باید
    یک میزبان تست دوم روی Postgres ساخته شود.
14. [x] موارد P2/P3 گزارش ممیزی — همه بسته شدند: آدرس پیش‌فرض بعد از حذف و بعد
    از برداشتن تیک، آزاد شدن کد تخفیف بعد از لغو، متن alt فارسی به‌جای پرامپت
    انگلیسی، خطای ۵۰۰ در صفحه‌بندی عمیق، og:image پیش‌فرض، و در این پاس دو مورد
    آخر: **مسابقه‌ی درخواست مرجوعی** (خواندن سفارش، شمردن ادعاهای قبلی و نوشتن
    درخواست حالا یک تراکنش با قفل روی ردیف سفارش است) و **مرجوعی بر اساس SKU**
    (ترکیب انتخابی از سفارش تا فرم مشتری تا انبار همه‌جا حمل می‌شود؛ قبلاً دو خط
    یک محصول با دو رنگ مختلف قابل تفکیک نبودند).

### باز مانده از کار ایمیل‌ها

قالب‌هایی که آماده‌اند و منتظر عملیاتی هستند که هنوز کامل نیست:

- **پردازش مرجوعی توسط اپراتور.** عملیات و صفحه‌ی پنل هر دو ساخته شدند، پس قالب «مرجوعی بازپرداخت شد» دیگر منتظر هیچ قابلیت غایبی نیست — فقط باید به `ReturnDecisionService` وصل شود، همان‌جا که الان اعلان درون‌برنامه‌ای ساخته می‌شود. ارسالش هم مثل بقیه باید **بعد از commit شدن تراکنش** انجام شود، نه داخلش.
- **صدور پیش‌فاکتور B2B.** درخواست سازمانی ثبت می‌شود ولی جایی برای صادر کردن پیش‌فاکتور نیست. قالب «پیش‌فاکتور آماده شد» هم آماده است. این یکی هنوز واقعاً غایب است.

## این پاس: ۹ فاز ممیزی، شش دسته

فهرستی که از اسکن پروژه درآمد در شش کامیت بسته شد. چیزهایی که در آن فهرست بودند
ولی از قبل درست بودند هم اینجا ثبت شده‌اند تا دوباره دنبالشان نگردیم.

- **امنیت** — security stamp برای اپراتورها (تغییر گذرواژه حالا نشستِ باز را
  می‌بندد؛ قبلاً تا ۸ ساعت باز می‌ماند)، بستن لیست مشتریان روی نقش کاتالوگ،
  بررسی origin روی هر نوشتن در هر دو app (CSRF دیگر فقط به SameSite تکیه ندارد)،
  و کوکی امضاشده‌ی چت. **کوکی maintenance از قبل `Secure` داشت.**
- **دیتابیس** — ۴۶ کلید خارجی اضافه شد (از ۱۳ به ۵۹). دو ستون عمداً بدون کلید
  ماندند و دلیلش کنارشان نوشته شده: `Order.ShippingAddressId` (آدرس hard-delete
  می‌شود و snapshot همان چیزی است که تاریخچه می‌خواند) و `StockMovement.ActorId`
  (می‌تواند اپراتور باشد یا مشتری). به‌علاوه صفحه‌بندی عمیق، تولید slug یکتا با یک
  کوئری به‌جای ۹۹۸، SKU خالی روی فاکتورهای قدیمی، سقف و یکتاسازی روی grid نقش‌ها،
  و بازیابی jobهای معلق.
- **پول و سبد** — تخفیف چسبنده، قیمت‌های کهنه، بازپرداخت اشتباه COD، آزاد شدن
  کوپن، نشت سبد بعد از logout، مسابقه‌ی ثبت‌نام OTP، آدرس پیش‌فرض، صفحه‌بندی کیف
  پول.
- **بکاپ** — از «گزارش موفقیت» به دامپ واقعی.
- **کارایی** — درگاه بیرون از تراکنش، ایندکس trigram برای جست‌وجو، حذف N+1 لیست
  سفارش‌ها، و درخواست `logo.svg` که هرگز وجود نداشت.
- **سئو و رابط** — عبور دادن خطای واقعی به کاربر، og:image، alt فارسی.
  **canonical و sitemap از قبل درست بودند.**

## پاس دوم: باقی‌مانده‌ها

- **صفحه‌ی مرجوعی‌ها** ساخته شد — بزرگ‌ترین شکاف کاربری که مانده بود.
- **ایمیل واقعاً ارسال می‌شود** (SMTP روی حساب صندوق پستی).
- **کش سقف گرفت** و خواندن‌های کاتالوگ rate limit.
- **دو مورد آخر P2/P3** بسته شدند (مسابقه‌ی مرجوعی، مرجوعی بر اساس SKU).
- **advisoryها صفر شدند** و **CI اضافه شد**، با replay مهاجرت‌ها روی Postgres واقعی.
- **پیگیری سفارش** از قبل کامل بود؛ آیتمش کهنه بود.

## پاس سوم: درگاه، پیامک، و صفحه‌هایی که چیزی را تنظیم نمی‌کردند

هر دو موردی که «منتظر تصمیم» بودند بسته شدند — زرین‌پال و SMS.ir — و هر دو از
پنل تنظیم می‌شوند نه از فایل محیطی. کنارشان چند چیز پیدا شد که همان جنس اشکال را
داشتند: چیزی که وجود داشت و به بقیه‌ی سیستم وصل نبود.

- **درگاه پرداخت (زرین‌پال) و سرویس پیامک (SMS.ir)** — بالا توضیح داده شدند.
- **هشت صفحه‌ی تنظیمات از هیچ‌جای پنل قابل رسیدن نبودند** — نه لینکی در سایدبار و
  نه از صفحه‌ای دیگر. تنها راه باز کردنشان دانستن آدرس بود، و دوتایشان (پرداخت و
  ارسال) اولین کاری بودند که یک مالک تازه باید انجام دهد. حالا در سایدبارند.
- **تنظیمات ارسال هیچ‌وقت چیزی را تغییر نمی‌داد.** سه قیمت را در جدول عمومی
  settings می‌نوشت که هیچ‌کس نمی‌خواندش، و مبلغی که واقعاً از مشتری گرفته می‌شد از
  ردیف‌های `ShippingMethod` می‌آمد که فقط seeder نوشته بودشان — یعنی فروشگاهی که
  کرایه‌ی پیکش گران شده بود باید دوباره deploy می‌شد. حالا همان ردیف‌ها را ویرایش
  می‌کند.
- **سوییچ‌های روش پرداخت** هم همین‌طور بودند و حالا `PaymentMethod.IsActive` را
  می‌زنند.
- **صفحه‌هایی که هیچ چیزی را تنظیم نمی‌کردند حذف شدند** (ظاهر، سئو، اعلان‌های
  ادمین، اتصال‌ها). آخری بعد از ساخته شدن صفحه‌ی واقعی پیامک، فعالانه گمراه‌کننده
  شده بود: سه سرویس‌دهنده‌ای را پیشنهاد می‌داد که هیچ‌کدام پیاده نشده بودند.
  سوییچ «خرید بدون ثبت‌نام» هم برداشته شد — روشن نشان داده می‌شد و تسویه‌حساب
  همیشه به حساب کاربری نیاز داشته است.
- **کانال ایمیل کمپین‌ها فعال شد** (ارسال واقعی از همان صندوق پشتیبانی) و
  **fan-out پیامک و ایمیل قابل ازسرگیری شد** (`NotificationDelivery`). این یکی
  با آمدن سرویس واقعی از یک آزار به یک اشکال مالی تبدیل شده بود: شکست در وسط
  ارسال، دفعه‌ی بعد همان پیام را دوباره برای همه‌ی کسانی می‌فرستاد که گرفته
  بودند.
- **وضعیت پرداخت سفارش در پنل دیده نمی‌شد و ثبت هم نمی‌شد.** ستون، مهاجرت،
  قاعده‌ی «سفارش پرداخت‌نشده ارسال نمی‌شود» و تست‌های دامنه‌اش همه وجود داشتند و
  هیچ صفحه‌ای آن را نمی‌خواند و هیچ دکمه‌ای نمی‌نوشت — یعنی سفارش «پرداخت در محل»
  که پیک پولش را گرفته بود تا ابد «در انتظار پرداخت» می‌ماند، و صف مرجوعی همین
  را می‌خواند تا تصمیم بگیرد بازپرداختی هست یا نه. حالا روی صفحه‌ی سفارش
  کارت «وضعیت پرداخت» هست.
- **سقف عمومی rate limit** اضافه شد، چون «نامحدود مگر اینکه در فهرست باشد» برای
  یک API عمومی پیش‌فرض غلطی است. به‌علاوه سقف زمان اجرای هر کوئری، و در لبه:
  `limit_req`/`limit_conn` و تایم‌اوت‌های slowloris در nginx.

- **advisoryها دوباره به صفر رسیدند** — از ۱۱ مورد (۲ بحرانی، ۵ high) به صفر در
  هر دو درخت. یکی از آن‌ها (`nanoid` از دل `next`) در درخت production بود. بقیه
  ابزار توسعه بودند ولی حذف شدند نه توجیه: `vitest` به ۳.۲.۶ رفت (که هم مورد
  بحرانی خودش را می‌بندد و هم اجازه می‌دهد `vite` و `esbuild` پین شوند — پین کردن
  `vite` روی vitest 2 کل تست‌ها را می‌شکست). سمت دات‌نت هم
  `SQLitePCLRaw` به ۳.۰.۵ رسید و `NoWarn` مربوط به NU1903 برداشته شد؛ دانلودی که
  قبلاً روی این شبکه نصفه می‌ماند این بار کامل شد.

  **یک نکته‌ی مهم برای هرکسی که این مخزن را clone می‌کند**: روی این ماشین
  `node_modules` از Next 15.1.6 مانده بود در حالی که `package.json` و lockfile
  هر دو 15.5.21 را می‌گفتند — یعنی build و تست محلی روی نسخه‌ای اجرا می‌شد که
  CVE-2025-29927 را دارد، همان چیزی که محافظت مسیرها در middleware را دور می‌زند.
  خودِ lockfile درست بود پس چیزی که deploy می‌شود سالم بود، ولی
  `pnpm install --frozen-lockfile` بعد از هر تغییر نسخه لازم است.

## پاس چهارم: یک‌دستی، ریسپانسیو، و یک دور امنیتی دیگر

این پاس دنبال «چیزی که دو بار نوشته شده» گشت، چون هرجا یک قاعده دو نسخه دارد
دیر یا زود یکی‌شان درست می‌ماند و دیگری نه — و همان اتفاق چند بار افتاده بود.

- **سوییچ شش نسخه داشت**، پنج‌تا کاراکتر‌به‌کاراکتر یکی. ششمی (پنل تخفیف محصول)
  جهت حرکت دستگیره‌اش برعکس بود، یعنی دقیقاً **خلاف وضعیت خودش** را نشان می‌داد.
  دو نسخه‌ی دیگر کامنت داشتند که همین باگ در آن‌ها پیدا و رفع شده — که روشن‌ترین
  دلیل ممکن است بر اینکه رفعِ یک نسخه به بقیه نمی‌رسد. هیچ‌کدام هم حالت focus
  نداشتند.
- **خط وضعیت فرم بیست‌وهشت نسخه داشت** و یکی‌شان (تنظیمات فاکتور) هم اندازه‌اش
  فرق کرده بود و هم خطا را با `aria-live="polite"` اعلام می‌کرد نه `role="alert"`.
- **یازده آیکون در فونت نبودند** و اسم خودشان را به‌صورت متن رندر می‌کردند —
  از جمله چشمکِ نمایش گذرواژه در صفحه‌ی ورود پنل. اسکنر فونت فقط اسم‌های
  به‌شکل رشته‌ی ساده را می‌دید و `name={a ? 'x' : 'y'}` را نمی‌دید؛ **تستی هم که
  دقیقاً برای همین نوشته شده بود، کپی دستی همان الگوها را داشت**، پس همان نقطه‌کور
  را داشت. حالا تست، خودِ collector اسکریپت را import می‌کند.
- **فونت آیکون با `immutable` و یک‌سال کش روی آدرسی سرو می‌شد که هرگز عوض نمی‌شد**،
  یعنی ساختن دوباره‌ی فونت برای هیچ بازدیدکننده‌ی قبلی اثری نداشت. حالا هش محتوا
  در آدرس نوشته می‌شود.
- **صفحه‌ی جزئیات سفارش روی موبایل ۲۲۵ پیکسل سرریز داشت**: یک grid item به‌طور
  پیش‌فرض `min-width: auto` است و زیر عرض محتوایش جمع نمی‌شود، پس
  `overflow-x-auto` جدول هیچ‌وقت عرضی برای اسکرول پیدا نمی‌کرد و کل صفحه کش
  می‌آمد. در هر شش جایی که این چیدمان تکرار شده بود اصلاح شد.
- **دو صفحه‌ی عمومی برای کاربر مهمان ۵۰۰ می‌دادند** (`/loyalty` و `/offers`) چون
  یک endpoint زیر `/me` صدا می‌زدند و ۴۰۱ را رد نمی‌کردند. یعنی هر بازدیدکننده‌ی
  بدون حساب و هر خزنده‌ی موتور جستجو خطای سرور می‌گرفت.
- **کانال ایمیل کمپین از سمت API رد می‌شد** در حالی که پنل و dispatcher هر دو
  فعالش کرده بودند — کاشف به عمل آمد که گاردِ «کانالی که provider ندارد» هنوز
  ایمیل را در فهرستش داشت. حالا تست می‌گوید هر کانالی که composer پیشنهاد می‌دهد
  باید پذیرفته شود.
- **`safeNextPath` و `isSameOriginRequest` هرکدام دو نسخه داشتند** — دو بررسی
  امنیتی، و فقط نسخه‌ی فروشگاه تست داشت. هر دو به `@bojan/config` منتقل شدند.
- **گارد «حداقل یک روش ارسال فعال بماند»** فقط فهرست ارسالی را می‌سنجید، پس
  خاموش کردن روش‌ها یکی‌یکی از کنارش رد می‌شد و فروشگاه بدون هیچ روش ارسالی
  می‌ماند. حالا وضعیت نهایی سنجیده می‌شود.

ممیزی ریسپانسیو با اندازه‌گیری واقعی روی ۳۲۰، ۳۷۵ و ۷۶۸ پیکسل انجام شد (نه با
خواندن کد): بعد از این اصلاح‌ها هیچ سرریز افقی در هیچ صفحه‌ای از فروشگاه و پنل
باقی نماند.

سه موردی که آخر این پاس عمداً باز ماند — تنظیمات فروشگاهی که هیچ صفحه‌ای
نمی‌خواند، صدور پیش‌فاکتور B2B، و کانال پوش — در پاس پنجم بسته شدند.

## پاس پنجم: پوش، پیش‌فاکتور، درگاه‌های بیشتر، و پایان دادن به مقدارهای ثابت

- **تست‌ها روی دیتابیسی اجرا می‌شدند که فروشگاه با آن کار نمی‌کند.** کل تست‌های
  API روی SQLite بودند در حالی که فروشگاه روی PostgreSQL است — و این «یک
  دیتابیس با دو املا» نیست: قفل ردیفی که سبد خرید به آن وابسته است در SQLite
  اصلاً کاری نمی‌کند، پس `FOR UPDATE` حتی یک‌بار هم توسط هیچ تستی اجرا نشده بود و
  تست‌های ادعای هم‌زمان موجودی روی موتوری سبز می‌شدند که اصلاً نمی‌تواند آن‌طور
  شکست بخورد. حالا یک کانتینر واقعی PostgreSQL بالا می‌آید، یک‌بار مهاجرت‌ها روی
  یک دیتابیس الگو اجرا می‌شوند و برای هر کلاس تست یک کپی از آن الگو ساخته
  می‌شود.
- **دو درگاه دیگر کنار زرین‌پال**: زیبال و آیدی‌پی، هر دو پشت همان پورت و
  انتخاب‌شونده از پنل. این‌ها سه نسخه از یک API نیستند: زیبال به ریال حساب
  می‌کند و سندباکسش **مرچنت** را عوض می‌کند نه آدرس را (اشتباه گرفتنش یعنی
  کارت واقعی کشیده می‌شود)، و آیدی‌پی روی **جفتِ** شناسه و شماره‌ی سفارش تأیید
  می‌کند — به همین دلیل پورت حالا شماره‌ی سفارش را هم حمل می‌کند، چون تأیید فقط
  با reference یعنی پذیرفتن callbackای که پرداخت شخص دیگری را نام می‌برد. ضمناً
  **مرجع شارژ کیف پول یکتا نبود** و دو شارژ هم‌زمان یک مشتری برای درگاه یک پرداخت
  بودند، پس تأیید دومی اولی را تسویه می‌کرد.
- **پنل می‌گفت پیش‌فاکتور صادر شد و هیچ پیش‌فاکتوری صادر نمی‌کرد** — دکمه فقط
  وضعیت را روی `quoted` می‌گذاشت. حالا اپراتور اقلام و تعداد را می‌دهد و **قیمت‌ها
  هرگز از بدنه‌ی درخواست خوانده نمی‌شوند**: هر قیمت واحد از کاتالوگ و هر تخفیف از
  نردبان پلکانی خود محصول می‌آید. کنارش **تخفیف پلکانی سازمانی** اضافه شد: از
  بیست عدد ده درصد، از صد عدد هجده درصد — با پیش‌نمایش به تومان، چون درصد را
  آسان می‌شود تایپ کرد و سخت می‌شود تصور کرد. پله‌ها **کف** هستند نه بازه (بازه‌ها
  را باید دستی چسبیده نگه داشت و فاصله‌ی بینشان یعنی تعدادی که قیمت ندارد) و هر
  پله باید از پله‌ی قبل تخفیف بیشتری بدهد، وگرنه سفارش بزرگ‌تر گران‌تر تمام
  می‌شود.
- **پنج صفحه از شش صفحه‌ی ویرایش محصول از هیچ‌جا لینک نبودند** — قیمت‌گذاری،
  تخفیف، کدهای انبار، ویژگی‌ها و تصاویر همه ساخته شده بودند و فقط با دانستن
  آدرسشان باز می‌شدند.
- **اعلان مرورگر**: کاشی‌اش در پنل بود، غیرفعال و با برچسب «به‌زودی». سرویس‌دهنده‌ای
  هم لازم ندارد — پیام مستقیم به سرویس خود مرورگر می‌رود و تنها اعتبارنامه‌ی
  فروشگاه یک جفت کلید است که خودش می‌سازد. رمزنگاری پیام طبق RFC 8291 و هدر هویت
  طبق RFC 8292 **از روی خود استانداردها** نوشته شد نه از روی یک پکیج (هر
  primitive لازم در BCL هست، پس وابستگی یعنی یک شخص ثالث دیگر داخل مسیری که خبر
  سفارش مشتری از آن می‌گذرد) و **بایت‌به‌بایت با مثال کاری خود RFC** تست می‌شود —
  رمزنگاری‌ای که کمی اشتباه باشد درخواستی می‌سازد که سرویس می‌پذیرد و مرورگر در
  سکوت دور می‌ریزد.
- **هیچ عدد و هیچ متنی در فروشگاه نباید بدون رفتن به کد قابل تغییر نباشد.** نام
  فروشگاه، تماس، آدرس، شبکه‌های اجتماعی، **سقف ارسال رایگان**، **مهلت مرجوعی** و
  **زمان تقریبی تحویل** همه از صفحه‌ی تنظیمات خوانده می‌شوند. این‌ها قبلاً در
  کامپوننت‌ها نوشته شده بودند، چندتایشان در بیش از یک جا و با عددهای متفاوت. سال
  کپی‌رایت فوتر هم عدد ثابت ۱۴۰۵ بود.
- **صفحه‌های اطلاعاتی (قوانین، حریم خصوصی، شرایط ارسال و مرجوعی، راهنماها) داخل
  باندل کامپایل می‌شدند** — یعنی متنی که مشتری با خرید کردن آن را می‌پذیرد و
  سیاستی که فروشگاه به آن پایبند است بدون توسعه‌دهنده و بدون deploy قابل اصلاح
  نبود. حالا از CMS خوانده می‌شوند و متن فعلی به‌عنوان **پیش‌فرض** می‌ماند: فروشگاهی
  که چیزی ننوشته صفحه‌ی خالی ندارد، و لحظه‌ای که مالک صفحه‌ای را ذخیره کند همان
  جای این را می‌گیرد.
- **هر چهار نوع محتوای پنل حالا خوانده می‌شوند.** «صفحات ثابت»، «سوالات متداول»
  و «بنرها» هر سه ویرایشگر داشتند و هیچ‌چیز در فروشگاه سطرهایشان را نمی‌خواند —
  یعنی هر پرسشی که اپراتور می‌نوشت به جایی نمی‌رفت و عنوان بزرگ بالای صفحه‌ی
  اصلی فقط با ویرایش کامپوننت عوض می‌شد. برای هر سه، متن فعلی به‌عنوان پیش‌فرض
  می‌ماند.
- روی صفحه‌ی «سفارش ثبت شد» روش ارسال همیشه «ارسال استاندارد» نوشته می‌شد،
  هرچه مشتری انتخاب کرده بود.

### چیزی که سر راه پیدا شد و عمداً دست‌نخورده ماند

**باشگاه مشتریان یک برنامه‌ی نمایشی است، نه یک برنامه.** صفحه‌ی `/loyalty` سه
پله با آستانه و مزایا نشان می‌دهد («۵٪ تخفیف دائمی»، «ارسال رایگان نامحدود») و
هیچ‌کدام در هیچ‌جای کد اعمال نمی‌شوند: `AddLoyaltyPoints` تنها یک فراخوانی در کل
پروژه دارد و آن هم در seeder است، پس هیچ سفارشی امتیاز نمی‌دهد و امتیاز هیچ
مشتری‌ای هرگز بالا نمی‌رود.

عمداً پله‌ها را «از پنل قابل تغییر» نکردیم، چون قابل تغییر کردنِ عددی که هیچ‌چیز
به آن عمل نمی‌کند فقط این توهم را محکم‌تر می‌کند که برنامه‌ای هست. این یک قابلیت
است نه یک تنظیم: باید امتیاز روی سفارش تحویل‌شده حساب شود، پله از روی امتیاز
تعیین شود و تخفیفش سر پرداخت واقعاً کسر شود. تا آن‌وقت، تصمیم با کارفرماست که
صفحه بماند یا برداشته شود.

## پاس ششم: سرور واقعی — ظرفیت، کش، و محافظت‌هایی که هرگز اعمال نشده بودند

این پاس روی خودِ سرور انجام شد، نه روی کد. همه‌ی اعداد اندازه‌گیری‌شده‌اند.

### ظرفیت اندازه‌گیری‌شده (۲ هسته، ۳.۷ گیگ رم)

| مسیر | قبل | بعد |
|---|---|---|
| صفحه‌ی اصلی (مهمان) | ۲۲ req/s | ۲,۸۷۵ |
| صفحه‌ی اصلی (وارد‌شده) | ۲۱ req/s | ۲,۷۷۲ |
| لیست محصولات | — | ۲,۵۳۰ |
| صفحه‌ی محصول | — | ۳,۷۸۰ |
| API خواندنی | ۵,۰۰۰–۶,۲۰۰ | بدون تغییر |
| رندر بدون کش (سبد، حساب) | ۲۱ req/s | ۲۱ req/s |

یعنی **حدود ۲۰ تا ۲۵ هزار کاربر همزمان برای گشت‌وگذار، ولی فقط ~۲۵۰ سفارش در دقیقه.**
مسیر پرداخت ذاتاً کش‌شدنی نیست و همان سقف را دارد. برای کمپین ۱۰۰ هزار نفری
لازم است: CDN جلوی سایت، ۸ تا ۱۶ هسته برای اپ، و پستگرس روی ماشین جدا.

- **گلوگاه API نبود.** بک‌اند ۵ تا ۶ هزار درخواست در ثانیه جواب می‌داد و صفحه‌ی
  اصلی ۲۲ تا. علت: `force-dynamic` باعث می‌شد React برای هر بازدیدکننده کل صفحه
  را از نو رندر کند. توضیحی که بالای آن نوشته شده بود درست می‌گفت چرا نباید در
  زمان build رندر شود، ولی نتیجه‌اش («چیزی از دست نمی‌رود») غلط بود: داده‌ها کش
  می‌شدند، رندر نه.
- **کش کوتاه‌مدت nginx** اضافه شد، با `proxy_cache_lock` — همان خطی که تعیین
  می‌کند هجوم کمپین جذب شود یا تشدید. صفحات شخصی (`/account`، `/checkout`،
  `/loyalty`) هرگز کش نمی‌شوند و تأیید شد که هیچ هدر کشی نمی‌گیرند.
- **دور زدن کش برای کاربر وارد‌شده برداشته شد.** محتاطانه به‌نظر می‌رسید ولی
  یعنی هر کسی که وارد شده بود ۲۱ req/s می‌گرفت و مهمان‌ها ۲,۸۰۰ — و در کمپین،
  کسانی که وارد می‌شوند همان‌هایی هستند که می‌خرند. فروشگاه هیچ داده‌ی کاربر را
  سمت سرور رندر نمی‌کند (زنگ اعلان عمداً کلاینتی است)، پس HTML یکی است.

### محافظت‌هایی که نوشته شده بودند و روی سرور نبودند

`deploy/install.sh` همه‌شان را تولید می‌کند و این نصب هیچ‌کدام را نداشت:
`limit_req`، `limit_conn`، تایم‌اوت‌های slowloris، `proxy_read_timeout`، و فایل
zoneها. یعنی همان دیداسی که از روز اول نگرانش بودیم، کدش ماه‌ها پیش کامیت شده
بود و در پروداکشن وجود نداشت — بدترین حالت ممکن برای یک دفاع، چون همه فکر
می‌کنند هست.

اعدادش برای واقعیت ایران تنظیم شد: `limit_conn 40` یعنی هفت کاربر پشت NAT
اپراتور موبایل و رد کردن هشتمی. به ۵۰۰ و نرخ صفحه به ۲۰۰r/s رسید.

### امنیت سرور

- **۴,۴۱۷ تلاش ناموفق ورود SSH در ۲۴ ساعت**، که ۱,۷۴۸ تای آن روی `root` بود.
  fail2ban نصب نبود و فایروال خاموش بود. هر دو راه افتادند (fail2ban در همان
  ثانیه‌های اول سه مهاجم را بست) و آی‌پی مالک در لیست سفید است تا یک رمز اشتباه
  کسی را از سرور خودش بیرون نیندازد.
- **ورود root با رمز هنوز باز است** و عمداً دست نخورد: تنها کلید روی سرور کلیدی
  است که برای همین کار ساخته شد، و بستن ورود با رمز یعنی قفل شدن مالک بیرون.
  این تصمیم با کارفرماست.
- بقیه سالم: فقط ۲۲/۸۰/۴۴۳ عمومی‌اند، پستگرس و API فقط روی لوکال‌هاست، هدرهای
  امنیتی کامل، پنل با `noindex` و بدون نشست غیرقابل دسترس، هیچ رازی در ریپازیتوری،
  و صفر آسیب‌پذیری در وابستگی‌های هر دو طرف.
- **۷۳۶ کوئری SQL در ۱۰ دقیقه در لاگ پروداکشن** — دیسک را می‌سوزاند، شمای
  دیتابیس را می‌نویسد، و خطای واقعی را گم می‌کند. Serilog بخش خودش را می‌خواند نه
  `Logging:LogLevel` را، پس override لازم بود.

## انجام‌شده

- [x] نیمه‌ی اپراتور مرجوعی‌ها: نیمه‌ی مشتری از فاز ۵ ساخته شده بود ولی هیچ‌چیز نمی‌توانست درخواست را تکان بدهد — `ReturnRequest.TransitionTo` **هیچ فراخوانی نداشت**، پس هر مرجوعی تا ابد در «ثبت‌شده» می‌ماند و تایم‌لاین صفحه ۳۶ یک قدم اول را می‌کشید که هرگز جلو نمی‌رفت. حالا صف (`GET /admin/returns`)، جزئیات (`GET /admin/returns/{id}`) و تصمیم (`POST /admin/returns/decide`) هست. **مبلغ از بدنه‌ی درخواست گرفته نمی‌شود**: `ReturnRefund` آن را از قیمت‌های منجمدشده‌ی خود سفارش حساب می‌کند — سهم تخفیف سفارش به نسبت ارزش کالا کسر می‌شود (وگرنه کسی که با کوپن ۳۰٪ خرید کرده می‌توانست نصف ارزان سفارش را پس بدهد و بیشتر از قیمت نصف گران بگیرد)، و هزینه‌ی ارسال هرگز برنمی‌گردد چون بسته حمل و تحویل شده. مرجوعی روی سفارشی که پولش وصول نشده رد می‌شود (سفارش «پرداخت در محل»ِ تحویل‌شده‌ی تسویه‌نشده، حالت واقعی این مورد است) و صف قبل از کلیک با `payable` همین را نشان می‌دهد. برگشت کالا به انبار **قضاوت اپراتور** است نه نتیجه‌ی خودکارِ «دریافت شد»، چون بسته از دست فروشگاه خارج بوده و ممکن است آسیب‌دیده برگردد؛ فقط همان تعداد مرجوعی برمی‌گردد نه کل سطر سفارش. جابه‌جایی وضعیت هم مثل سفارش فقط رو به جلوست و مبدأ/اپراتور/دلیل را ثبت می‌کند، و رسیدن به «بازپرداخت‌شده» بدون نام بردن مبلغ ممکن نیست. سفارش تنها وقتی «بازپرداخت‌شده» علامت می‌خورد که همه‌ی واحدهایش برگشته باشند
- [x] وضعیت پرداخت سفارش: `Order.PaymentStatus` اضافه شد و عمداً از وضعیت ارسال جداست — یکی مسیر پول است و یکی مسیر کالا، و در یک ستون جمع کردنشان یعنی «ارسال‌شده» و «پرداخت‌شده» دیگر قابل تفکیک نیستند. الگو از فونیکس گرفته شد که سفارش‌هایش تا تأیید رسید توسط اپراتور در `PendingApproval` می‌مانند: بدون درگاه هم می‌شود وضعیت پرداخت درست داشت. `MarkPaid`/`MarkPaymentFailed`/`MarkRefunded` هر سه idempotent‌اند و زیر قفل ردیف سفارش داخل تراکنش صدا زده می‌شوند، پس دوبار کلیک کردن دکمه چیزی را دوبار تسویه نمی‌کند. هیچ سفارشی جز «پرداخت در محل» بدون پرداخت به «ارسال‌شده»/«تحویل‌شده» نمی‌رود — ولی **لغو** عمداً از این قاعده بیرون است، وگرنه هر سفارشی که این قاعده جلویش را گرفته برای همیشه گیر می‌افتاد. تایم‌لاین سفارش هم مثل `OrderStatusHistory` فونیکس مبدأ، اپراتور و دلیل را نگه می‌دارد
- [x] ایمیل‌های تراکنشی به مشتری: اول نیازسنجی شد (از روی دامنه‌ی خود بوژان، نه کپی از فونیکس) و ۱۴ ایمیل مشخص شد، بعد طرح زده و تأیید شد، بعد پیاده‌سازی. یک پوسته‌ی مشترک جدول‌محور با استایل inline (چون Gmail و Outlook با flex/grid و `<style>` بیرونی کار نمی‌کنند)، با رنگ‌های خود فروشگاه: هدر تیل عمیق `#003441`، دکمه‌ی مرجانی `#F36F5D`. **هیچ تصویری** — نه لوگو نه پیکسل — چون کلاینت‌ها تصویر را می‌بندند و ایمیل شکسته می‌رسد، ضمن اینکه تصویر بیرونی رسید خواندن است. هر ایمیل هم متن ساده دارد هم HTML. مبلغ‌ها با ارقام فارسی و جداکننده‌ی ASCII (مثل خود سایت)، شماره‌ی سفارش و فاکتور لاتین چون مرجع‌اند. تاریخ‌ها جلالی و به وقت تهران. **دو قانون که همه‌جا رعایت شده**: مشتری بدون ایمیل رد می‌شود نه خطا (مسیر اصلی ثبت‌نام موبایل است)، و شکست ارسال هرگز به درخواست اصلی نمی‌رسد (ثبت سفارش نباید چون سرور ایمیل قطع است شکست بخورد). به‌علاوه سه چیز که سر راه درست شد: بازیابی گذرواژه فقط **توکن خام** را می‌فرستاد بدون لینک و بدون قالب؛ `StockAlert.NotifiedAtUtc` از اول وجود داشت و هیچ‌وقت مقدار نمی‌گرفت، یعنی درخواست‌های «خبرم کن» جمع می‌شد و هیچ‌کس خبر نمی‌شد؛ و `IEmailSender` جای HTML نداشت
- [x] ایمیل‌ها (صندوق پستی پشتیبانی): از فونیکس مدل‌سازی و برای بوژان پیاده شد — ایمیل‌هایی که مشتری به نشانی پشتیبانی می‌فرستد در پنل خوانده و پاسخ داده می‌شود. IMAP برای دریافت و SMTP برای ارسال (`MailKit`)، پیام‌ها به‌صورت **گفتگو** گروه‌بندی می‌شوند (INBOX + Sent، با حذف پیشوندهای Re/Fwd/پاسخ/ارجاع)، و پاسخ با هدرهای `In-Reply-To`/`References` در همان رشته‌ی مشتری می‌نشیند و در پوشه‌ی Sent بایگانی می‌شود. **دو لایه امنیت روی متن ایمیل**: allow-list سمت سرور (`HtmlSanitizer` — فقط تگ/ویژگی/شِمای مجاز، حذف کامل تصاویر بیرونی چون رسید خواندن و لو رفتن IP هستند) و رندر در `iframe sandbox` بدون `allow-scripts` و بدون `allow-same-origin`. گذرواژه‌ی صندوق **رمزنگاری‌شده** ذخیره می‌شود (`IDataProtection`) — بقیه‌ی رازهای پروژه hash می‌شوند ولی این یکی باید به سرور IMAP بازپخش شود — و هرگز به مرورگر برنمی‌گردد. خواندن صندوق با نقش پشتیبانی، ولی تنظیمات فقط برای owner. پیوست‌ها همیشه به‌صورت دانلود (`octet-stream` + `nosniff`) سرو می‌شوند نه inline
- [x] نوتیفیکیشن: از فونیکس مدل‌سازی شد و ایرادهایش برطرف شد. **سه باگ زنده پیدا و رفع شد**: (۱) گزینه‌ی «اعلان درون‌برنامه‌ای» در پنل مقدار `push` می‌فرستاد نه `in-app`، پس کمپین به‌عنوان push ذخیره می‌شد و هیچ اعلانی به هیچ مشتری‌ای نمی‌رسید درحالی‌که پنل می‌گفت ارسال شد؛ (۲) `INotificationDispatcher` ثبت شده بود ولی هیچ‌چیز صدایش نمی‌زد، یعنی کمپین زمان‌بندی‌شده و هر ارسال پیامکی برای همیشه در صف می‌ماند — حالا `NotificationWorker` (مثل `ReportExportWorker`) صف را خالی می‌کند؛ (۳) کانالی که سرویس‌دهنده ندارد (ایمیل/پوش) به‌جای اینکه در سکوت دور ریخته شود، همان اول رد می‌شود. به‌علاوه: لینک اعلان حالا در دامنه اعتبارسنجی می‌شود (فقط مسیر داخلی — `//`، بک‌اسلش و کاراکتر کنترلی هم رد می‌شوند)، شمارنده‌ی خوانده‌نشده و زنگ اعلان در هدر فروشگاه اضافه شد، اپراتور می‌تواند به یک مشتری خاص اعلان بفرستد (کاری که فقط فونیکس داشت)، فید اعلان‌ها سقف‌دار شد، و fan-out انبوه دسته‌ای و **قابل ازسرگیری** شد (`CustomerNotification.CampaignId` + ایندکس یکتای شرطی — بدون آن، شکست وسط ارسال باعث می‌شد تلاش بعدی همان پیشنهاد را دوباره برای همه‌ی کسانی که قبلاً گرفته بودند بفرستد)
- [x] خطای ۵۰۰ روی پارامتر نامعتبر: `?page=abc` روی هر endpointای که پارامتر عددی داشت **۵۰۰** برمی‌گرداند — نه فقط بخش‌های جدید، بلکه همه‌ی لیست‌های صفحه‌بندی‌شده‌ی پنل. `BadHttpRequestException` خودش کد ۴۰۰ دارد ولی `UseExceptionHandler` همه‌ی استثناها را یکسان می‌دید. حالا یک middleware داخل آن، همین یک نوع را به ۴۰۰ تبدیل می‌کند و بقیه دست‌نخورده به هندلر و لاگ می‌رسند
- [x] فاکتور: از پروژه فونیکس (دات‌نت ۸) مدل‌سازی و با دات‌نت ۱۰ برای بوژان پیاده شد. شماره فاکتور ۱۶ رقمی و یکتا، دقیقاً در لحظه‌ی «تحویل‌شده» صادر می‌شود و هرگز دوباره صادر نمی‌شود (`Order.TransitionTo` + ایندکس یکتای شرطی). `InvoiceBuilder` در دامنه، فقط اقلام نگه‌داشته‌شده را فاکتور می‌کند: اقلامی که مرجوع و بازپرداخت شده‌اند از سطرها حذف و یک‌جا گزارش می‌شوند، و سهم آن‌ها از تخفیف و هزینه ارسال هم کسر می‌شود. سه endpoint (`GET /admin/invoices`، `GET /admin/orders/{id}/invoice`، `GET /me/orders/{id}/invoice`)، صفحه «مدیریت فاکتورها» با جستجو (شماره فاکتور با ارقام فارسی هم پیدا می‌شود)، و سند چاپی A4 مشترک بین فروشگاه و پنل (`InvoiceDocument`) با CSS چاپ که هدر/فوتر خود مرورگر را حذف می‌کند. صفحه ۳۴ فروشگاه که قبلاً فاکتور جعلی `INV-{شماره سفارش}` می‌ساخت، حالا به همین سند واقعی وصل است. مشخصات فروشنده، متن تشکر و شرایط، یادداشت پاورقی و مهر الکترونیک از «فاکتورها ← تنظیمات فاکتور» توسط مالک قابل تغییرند (مهر آپلود می‌شود و جای کادر خالی را می‌گیرد)؛ همه‌ی اعداد روی فاکتور لاتین‌اند
- [x] سرو فایل‌های آپلودشده: `LocalFileStorage` همیشه آدرس `/media/...` برمی‌گرداند ولی هیچ‌چیز به آن پاسخ نمی‌داد — یعنی هر آپلودی در پروژه (عکس محصول، رسید شارژ کیف پول، مهر فاکتور) ذخیره می‌شد و بعد ۴۰۴ می‌داد. حالا `Program.cs` همان پوشه را روی `Storage:PublicBaseUrl` سرو می‌کند، و فقط وقتی که آن یک مسیر باشد نه آدرس کامل (چون آدرس کامل یعنی CDN دارد سرو می‌کند)
- [x] وضعیت سرور در پنل ادمین (داشبورد): آپ‌تایم، CPU، حافظه، دیسک، سلامت پایگاه‌داده — بک‌اند دات‌نت ۱۰ (`GET /api/admin/system/status`) + کارت `ServerStatusCard` در داشبورد
- [x] حالت تعمیر و نگهداری: سوییچ پنل ادمین حالا واقعاً فروشگاه را می‌بندد (middleware فروشگاه + صفحه `/maintenance` + endpoint عمومی `/api/store/status`)؛ پیش‌نمایش با `?maintenance_bypass=<MAINTENANCE_BYPASS_SECRET>`
- [x] لایو چت: ویجت شناور در فروشگاه (بدون نیاز به ورود) + صفحه «گفتگوی آنلاین» در پنل ادمین زیر پشتیبانی — پیام‌رسانی واقعی روی دیتابیس، به‌روزرسانی هر ۴ ثانیه (polling، نه وب‌سوکت)
- [x] لوگو: کامپوننت `BrandLogo` اضافه شد — کافی‌ست فایل را در `frontend/apps/storefront/public/logo.svg` و `frontend/apps/admin/public/logo.svg` بگذارید (svg یا png)، بدون نیاز به تغییر کد جایگزین می‌شود؛ تا آن زمان همان وردمارک متنی «بوژان» نمایش داده می‌شود
- [x] منوهای پنل ادمین کشویی شدند — با کلیک روی عنوان هر گروه بسته/باز می‌شود، وضعیت در مرورگر ذخیره می‌ماند، و گروه حاوی صفحه فعلی همیشه باز است
- [x] عکس‌ها/متن‌های هیرو ریسپانسیو شدند: چند بخش (صفحه اصلی، صفحه‌های کمپین، کالکشن‌ها) ارتفاع ثابت + `overflow-hidden` داشتند و متن روی عکس با عنوان/توضیح بلندتر واقعاً از پایین قاب بریده می‌شد؛ حالا با `min-height` به‌جای `height` بزرگ می‌شوند و هیچ متنی برش نمی‌خورد
- [x] کش اضافه شد: `IMemoryCache` روی خواندنی‌های پرتکرار و کم‌تغییر کاتالوگ (دسته‌بندی‌ها، برندها، کالکشن‌ها) با TTL پنج دقیقه‌ای — عمداً روی قیمت/موجودی محصولات کش نگذاشتیم چون نباید کهنه نشان داده بشن
- [x] Celery: بک‌اند دات‌نته پس معادل واقعی‌اش پیاده شد، نه خود Celery. صف «خروجی گزارش» (`/reports/export`) از قبل در کد بود ولی هیچ‌وقت پردازش نمی‌شد (کامنت خود کد می‌گفت "worker fills in FileUrl" و هیچ workerای نبود) — حالا یک `BackgroundService` (`ReportExportWorker`) هر ۵ ثانیه صف را می‌خواند، فایل CSV واقعی از داده‌های گزارش می‌سازد، و لینک دانلود امن (`/admin/reports/export/{id}/download`) کار می‌کند. تست کامل شد: صف → پردازش → دانلود، همه واقعی. فرمت‌های XLSX/PDF فعلاً پیاده نشدن (خطای صریح می‌دن، نه یک CSV با اسم غلط)
- [x] لینک صفحات جزئیات با slug: بررسی کامل شد — صفحات عمومی/قابل‌جستجو (محصولات، برندها، دسته‌بندی‌ها، کالکشن‌ها، مقالات مجله) از قبل همه از slug استفاده می‌کنند. تنها لینک‌های ID-محور که باقی مونده صفحات خصوصیِ داخل حساب کاربری‌ان (سفارش، مرجوعی، استعلام قیمت B2B، آدرس) که `robots: noindex` دارن و اصلاً موجودیت `Slug` هم توی بک‌اندشون تعریف نشده — این‌ها عمداً ID-محور درستن (لینک عمومی/قابل‌اشتراک‌گذاری نیستن)، پس چیزی برای تغییر نبود.
- [x] منوهای پنل ادمین شبیه لینک فونیکس کشویی شدن: کل سایدبار حالا با یک دکمه به یک نوار باریک آیکون‌محور (rail) جمع می‌شه و دوباره باز می‌شه؛ وضعیت ذخیره می‌مونه و هدر بالای صفحه هم هماهنگ جابه‌جا می‌شه
- [x] **آسیب‌پذیری‌های بحرانی Next.js** — ارتقا از 15.1.6 به 15.5.21: دور زدن
  authorization در middleware (CVE-2025-29927) و RCE در پروتکل React flight.
  هر دو app کل محافظت مسیرهایشان را در middleware گذاشته‌اند، یعنی دقیقاً همان
  جایی که آن CVE از کار می‌انداخت. `pnpm audit --prod`: از ۳۶ مورد (۲ بحرانی،
  ۱۴ high) به ۵ مورد (۰ بحرانی) رسید. پنجره‌های ISR بعد از ارتقا دوباره اندازه
  گرفته شد و تغییر نکرده بود.
- [x] **لو رفتن کد ورود و توکن بازیابی رمز در لاگ** — `ConsoleSmsSender` و
  `ConsoleEmailSender` تنها پیاده‌سازی‌های موجود بودند و بدون هیچ شرطی ثبت
  می‌شدند، پس روی سرور واقعی هر کد ورود و هر توکن بازیابی در لاگ خودِ برنامه
  چاپ می‌شد. حالا متن پیام فقط در Development چاپ می‌شود و بدون
  `Notifications__AllowConsoleSenders` برنامه اصلاً بالا نمی‌آید.
- [x] **دسترسی غیرمجاز به گزارش مالی** — `/reports/financial` فقط برای owner بود
  ولی صف export برای همه‌ی نقش‌ها باز بود و نام گزارش هیچ اعتبارسنجی نداشت. یک
  اپراتور support می‌توانست `financial` را queue کند و دانلودش کند. حالا نام
  گزارش برابر `ReportCatalogue` بررسی می‌شود، نقش سنجیده می‌شود، و هر export فقط
  برای همان کسی که ثبتش کرده قابل دانلود است.
- [x] **قیمت ۱۰ برابر اشتباه در structured data** — `priceCurrency: 'IRR'` روی
  مبلغ تومانی، یعنی هر محصول با یک‌دهم قیمت واقعی به موتورهای خرید معرفی می‌شد.
- [x] **پنهان شدن محصولات با آرشیو کردن برند/دسته‌بندی** — join داخلی به جدولی که
  فیلتر soft-delete دارد، باعث می‌شد آرشیو یک برند تمام محصولاتش را از فروشگاه
  حذف کند در حالی که پنل همچنان آن‌ها را «منتشرشده» نشان می‌داد. بدون هیچ خطا و
  هیچ لاگی.
- [x] **آیکون‌های صفحه‌ی ورود پنل** — matcher میان‌افزار فونت آیکون را هم
  می‌گرفت و به `/login` ریدایرکت می‌کرد، پس در صفحه‌ای که هر اپراتور اول از همه
  می‌بیند به‌جای آیکون کلمه‌های `person`، `lock`، `visibility`، `login` و
  `security` نوشته می‌شد.
- [x] **کنتراست** — دکمه‌ی اصلی صفحه‌ی اول (سفید روی مرجانی) ۲.۶۱:۱ بود، حدود
  چهل درصد چیزی که WCAG AA لازم دارد؛ همین رنگ روی برچسب تخفیف هر کارت هم بود.
  اندازه‌گیری روی صفحه‌ی در حال اجرا: از ۴ ایراد به صفر.
- [x] **تزریق فرمول در خروجی CSV** — گزارش‌ها متنی را که کاربر ناشناس در فرم‌های
  عمومی وارد کرده حمل می‌کنند و فایل عمداً برای باز شدن در اکسل ساخته می‌شود.
- [x] **قفل و ترتیب تغییر وضعیت سفارش** — تنها نوشتنِ روی سفارش بود که نه تراکنش
  داشت و نه قفل، پس دو اپراتور هم‌زمان سفارشی به‌جا می‌گذاشتند که وضعیتش با
  تاریخچه‌ی خودش نمی‌خواند. قاعده‌ی «فقط رو به جلو» هم که در توضیح متد نوشته شده
  بود، در کد پیاده نشده بود.
- [x] **۵۰۰ شدن نقض unique constraint** — در کل بک‌اند حتی یک `DbUpdateException`
  گرفته نمی‌شد. حالا به ۴۰۹ نگاشت می‌شود و نام constraint و ستون در لاگ می‌ماند.
- [x] **سقف موجودی در سبد خرید** — افزودن به سبد به موجودی محدود می‌شد ولی تغییر
  تعداد در خود سبد نه، پس مشتری می‌توانست از کالای ۲ عددی، ۲۰ تا بگذارد و چهار
  صفحه بعد سر پرداخت رد شود.
- [x] **آدرس اشتباه در صفحه‌ی بررسی نهایی** — صفحه‌های ۷۷ و ۷۹ آدرس *پیش‌فرض* را
  نشان می‌دادند نه آدرسی که مشتری انتخاب کرده بود، در حالی که سفارش به آدرس
  انتخابی می‌رفت.
- [x] **کرش صفحه‌های checkout وقتی API در دسترس نیست** — سه صفحه
  `shippingMethods[0]!` می‌نوشتند، روی تابعی که در خطا `[]` برمی‌گرداند. همین
  ایراد قبلاً در `ChosenShippingCard` پیدا و رفع شده بود ولی سه صفحه‌ی همسایه جا
  مانده بودند.
- [x] **حالت تعمیر و نگهداری، ۲۰۰ به‌جای ۵۰۳** — صفحه‌ی maintenance با
  `noindex` روی وضعیت ۲۰۰ به معنی دستور *حذف* صفحه‌ها از ایندکس است. حالا ۵۰۳ با
  `Retry-After`.
- [x] **canonical** — فقط ۲ صفحه از ۸۹ صفحه canonical داشتند، پس هر لینک
  `?utm_source=…` یک نسخه‌ی تکراری قابل ایندکس بود.
- [x] **sitemap** — با یک خطای API کل sitemap ۵۰۰ می‌داد، و `pageSize: 500`
  به‌خاطر سقف ۱۰۰ سمت API به مقدار پیش‌فرض ۲۴ برمی‌گشت، یعنی sitemap عملاً ۲۴
  محصول داشت. حالا صفحه‌به‌صفحه خوانده می‌شود و هر خطا جداگانه تحمل می‌شود.
- [x] **کش فونت آیکون** — فایل‌های `public/` با `max-age=0` سرو می‌شدند، پس
  بزرگ‌ترین asset هر دو app در هر بار جابه‌جایی بین صفحه‌ها دوباره اعتبارسنجی
  می‌شد.
- [x] فونت آیکون دوباره ساخته شد (آیکون `developer_board` در subset نبود و تست
  قرمز بود).


## پاس هفتم: پنج موردی که «پیاده‌نشده» گزارش شدند

پنج مورد گزارش شد؛ سه‌تای اولش بسته شد و دوتای آخر باز مانده، چون بازماندنشان
ربطی به کد ندارد. «کد دارد» با «قابل استفاده است» یکی نیست، و معیار این فهرست
دومی است: قابلیتی که مالک نتواند کاری را که لازم دارد با آن انجام دهد، ناقص
است — چه سرویسش هنوز خریداری نشده باشد، چه دکمه‌اش وجود نداشته باشد.

1. [x] **سیستم لاگ‌گیری** — دو نیمه دارد و فقط یکی‌اش وجود داشت.

   **ممیزی (audit) از قبل کامل بود**: `AuditEntry`، پورت `IAuditLog`، حدود چهل
   فراخوان `audit.Record` در سرویس‌ها، و صفحه‌ی `/settings/audit` که آن‌ها را
   می‌خواند. ولی **از هیچ‌جای پنل لینک نداشت** — همان اشکالی که در پاس سوم برای
   هشت صفحه‌ی تنظیمات پیدا شد. تنها راه بازکردنش دانستن آدرس بود، پس کل
   تاریخچه‌ی فعالیت مثل قابلیتی به‌نظر می‌رسید که کسی ننوشته. حالا در سایدبار است.

   **لاگ خود برنامه واقعاً غایب بود**: Serilog فقط `WriteTo.Console()` داشت، بدون
   sink فایل. یعنی هرچه API درباره‌ی خودش می‌گفت به stdout می‌رفت و جای دیگری
   نمی‌ماند، و تنها راه خواندن دلیل یک خطا رسیدن به خود سرور و زدن `docker logs`
   بود. این همان چیزی است که برای پیدا کردن `DYNAMIC_SERVER_USAGE` صفحات
   دسته‌بندی لازم شد — یعنی خطایی که هر صفحه‌ی دسته‌بندی را خراب کرده بود، به‌جای
   اینکه از روی یک صفحه خوانده شود، توسط مشتری گزارش شد.

2. [x] **مدیریت کاربران** — انجام شد. هر دو کار در کارت «دسترسی حساب» روی
   صفحه‌ی مشتری، فقط برای owner.

   **بستن/باز کردن دسترسی.** `IsBlocked` از همان روزی که پنل ستون وضعیت داشت
   روی رکورد مشتری بود و **هیچ‌چیز آن را نمی‌نوشت** — در چهار جا خوانده می‌شد
   (فیلتر فهرست، نشان وضعیت، آمار مشتریان، و fan-out اعلان‌ها) و در هیچ‌جا
   اعمال نمی‌شد. یعنی پنل حالتی را نشان می‌داد که فروشگاه راهی برای قرار دادن
   کسی در آن نداشت، و ردیفی که مستقیم در دیتابیس بلاک می‌شد باز هم مثل قبل وارد
   می‌شد. حالا **هر چهار راه ورود بسته می‌شود**: کد یک‌بارمصرف، گذرواژه، لینک
   بازیابی (که اصلاً فرستاده نمی‌شود)، و لینکی که قبل از بستن صادر شده بود.
   بستن دسترسی security stamp را می‌چرخاند، پس نشست‌های باز همان لحظه می‌میرند —
   وگرنه پرچم فقط ورود بعدی را می‌گرفت و مرورگری که همین حالا وارد است، یعنی
   دقیقاً همانی که مهم است، دست‌نخورده می‌ماند. حذف نیست: سفارش‌ها، فاکتورها و
   کیف پول سر جایشان می‌مانند.

   **تعیین گذرواژه از پنل.** برای مشتری‌ای که نه پیامک به دستش می‌رسد و نه ایمیل
   بازیابی. همان سیاست گذرواژه‌ی خود مشتری اعمال می‌شود، نشست‌های باز بسته
   می‌شوند، و گذرواژه هیچ‌جا برنمی‌گردد و در لاگ نمی‌رود — خط ممیزی فقط می‌گوید
   این کار انجام شد و روی چه کسی.

   سیزده تست، بیشترشان روی اعمال‌شدن نه روی خود سوییچ: چهار راه ورود هست و
   تعلیقی که سه‌تا را ببندد، هیچ‌کدام را نبسته.

3. [x] **افزودن و مدیریت کاربر ادمین** — انجام شد. تا پیش از این
   `GET /settings/users` فقط فهرست می‌داد و هیچ اندپوینتی اپراتور نمی‌ساخت، پس
   فروشگاهی که آدم دومی در پنل لازم داشت **دقیقاً یک راه** داشت: دادن گذرواژه‌ی
   خود owner به او. هرچه پایین آمده برای جایگزینی همان یک راه است.

   **ساخت و ویرایش، با همان شکل upsert بقیه‌ی فرم‌های پنل.**
   `POST /admin/settings/users` بدون `id` می‌سازد و با `id` ویرایش می‌کند (نام،
   ایمیل، موبایل، نقش، فعال/معلق). ایمیل و موبایل هر دو هویت ورودند، پس یکتایی
   هر دو **قبل از** رسیدن به ایندکس بررسی می‌شود تا خطا به‌جای ۵۰۰، کنار همان
   فیلد بنشیند؛ و مقایسه‌ی ایمیل **بی‌توجه به بزرگی و کوچکی حروف** است، چون ورود
   همین‌طور نگاه می‌کند و بدون آن `Sara@` و `sara@` دو ردیفی بودند که ایندکس
   می‌پذیرد و لاگین یکی‌شان را دلبخواه برمی‌دارد.

   **گذرواژه‌ی اولیه و اجبار به تغییر در اولین ورود.** گذرواژه‌ای که owner
   می‌نویسد و بعد پشت تلفن می‌خواند، از همان لحظه‌ی ساخت **دو نفر** می‌دانندش و
   یکی‌شان صاحبش نیست. `AdminUser.MustChangePassword` همین پنجره را می‌بندد:
   `POST /auth/login` آن را برمی‌گرداند، کوکی پنل حملش می‌کند، و middleware تا
   وقتی پاک نشود اپراتور را روی صفحه‌ی «تغییر رمز عبور» نگه می‌دارد. تنها جایی که
   پاکش می‌کند `POST /me/password` است — یعنی همان‌جایی که گذرواژه‌ی فعلی پرسیده
   می‌شود.

   **تعلیق به‌جای حذف.** `AdminUser` کلید خارجی ردیف‌های ممیزی، کلیدهای API،
   حرکت‌های انبار و پیش‌فاکتورهاست و بیشترشان `Restrict`اند، پس حذف یا روی
   دیتابیس می‌شکند یا — اگر نمی‌شکست — سابقه‌ی کارهای همان آدم را با خودش می‌برد.
   تعلیق همه‌ی درها را می‌بندد و اسم او روی کارهایی که کرده می‌ماند.

   **دو قفلی که پنل را از بی‌صاحب شدن نگه می‌دارد.** تعلیق حساب خودت همیشه رد
   می‌شود (همان لحظه بیرون می‌افتی و صفحه‌ای که می‌توانست برت گرداند فقط از دست
   دیگری برمی‌آید)، و **آخرین owner فعال** نه تنزل می‌گیرد نه تعلیق — نه به دست
   خودش، نه به دست کسی دیگر. ولی owner ای که جانشین تعریف کرده **می‌تواند** کنار
   برود؛ قاعده درباره‌ی آخرین مالک است، نه درباره‌ی مالک‌ها.

   **دو نجات برای حسابی که نمی‌تواند وارد شود.**
   `POST /admin/settings/users/password` گذرواژه‌ی اپراتور دیگری را تعیین می‌کند و
   `POST /admin/settings/users/two-factor` ورود دو مرحله‌ای‌اش را خاموش می‌کند
   (برای authenticator ای که روی گوشی گم‌شده بود). **هیچ‌کدام روی حساب خودِ
   درخواست‌دهنده کار نمی‌کند**: نسخه‌ی self هر دو، گذرواژه‌ی فعلی یا کد فعلی
   می‌پرسد، و همین تنها چیزی است که بین یک نشستِ دزدیده‌شده و حسابِ پشتش ایستاده.
   هر دو security stamp را می‌چرخانند و راز TOTP پاک می‌شود نه اینکه کنار یک پرچم
   خاموش بماند.

   **یک ایراد قدیمی سر راه بسته شد:** `POST /me/password` همیشه stamp را
   می‌چرخاند، ولی پنل بعدش کوکی را نگه می‌داشت — یعنی اپراتوری که گذرواژه‌اش را
   عوض می‌کرد، پیام «انجام شد» می‌گرفت و از آن به بعد هر صفحه‌ای که باز می‌کرد
   ۴۰۱ می‌خورد بدون هیچ توضیحی. حالا کوکی همان‌جا دور ریخته می‌شود و صفحه‌ی ورود
   می‌آید.

   سی و هفت تست، بیشترشان روی همین قفل‌ها نه روی خود نوشتن: نوشتن یک ردیف و یک
   هش است، قفل‌ها همان چیزی‌اند که نمی‌گذارند این صفحه راهی باشد برای بی‌مالک
   کردن پنل یا برای اینکه یک نشست خودش را ترفیع دهد.

4. [ ] **پنل پیامک — ناقص.** کد سمت ما نوشته شده (SMS.ir،
   `GET/POST /sms/settings`، ارسال آزمایشی)، ولی **پنل سرویس هنوز خریداری
   نشده**. تا وقتی حساب واقعی نباشد هیچ پیامکی ارسال نمی‌شود و هیچ‌چیز از این
   مسیر روی سرویس واقعی تأیید نشده است. باز می‌ماند تا حساب تهیه شود و
   بعد end-to-end آزمایش شود: کد ورود روی خط خدماتی و قالب تأییدشده، و کمپین روی
   خط تبلیغاتی.

5. [ ] **درگاه پرداخت — ناقص.** کد سمت ما نوشته شده (زرین‌پال، زیبال، آیدی‌پی،
   `GET/POST /payment/settings` و تست اتصال)، ولی **پنل درگاه هنوز خریداری
   نشده**. یعنی هیچ پرداخت واقعی‌ای تا امروز از این مسیر نگذشته و حلقه‌ی
   پرداخت→بازگشت→تأیید روی درگاه واقعی تأیید نشده است. باز می‌ماند تا شناسه‌ی
   پذیرنده‌ی واقعی گرفته شود و یک خرید کامل با مبلغ کم آزمایش شود.

### آنچه در این پاس ساخته شد

- **sink فایل** کنار کنسول: چرخش روزانه، ۱۴ فایل، هرکدام تا ۳۲ مگابایت — پس بدترین
  حالت مصرف دیسک کران‌دار است و قدیمی‌ترین چیز خواندنی دو هفته پیش. روی volume
  جدا (`api-logs`) نصب می‌شود، چون ری‌استارتی که بعد از یک کرش می‌آید وگرنه
  خودِ سابقه‌ی کرش را هم می‌برد.
- **صفحه‌ی «لاگ سرور»** در پنل: انتخاب فایل، جستجوی متنی، و رنگ‌بندی سطرها بر اساس
  سطح (`ERR`/`WRN`) — چون چیزی که آدم دنبالش می‌گردد در دیواری از خطوط هم‌شکل گم
  می‌شود.
- **فقط خواندنی و فقط owner.** حذف ندارد: اپراتوری که بتواند لاگ را پاک کند
  می‌تواند سابقه‌ی کاری که کرده را پاک کند، و این دقیقاً همان چیزی است که لاگ برای
  جلوگیری از آن هست. نگه‌داشت با sink است نه با دکمه.
- **محافظت از path traversal** تنها نقطه‌ی امنیتی این صفحه است و یک choke point
  دارد: نام به `Path.GetFileName` تقلیل می‌یابد، پسوند از allow-list رد می‌شود، و
  مسیر نهایی دوباره بررسی می‌شود که هنوز داخل پوشه‌ی لاگ باشد. نبودن فایل و تلاش
  برای بیرون‌رفتن از پوشه عمداً یک جواب می‌گیرند (۴۰۴)، چون تفکیکشان به کسی که
  دارد کورمال می‌گردد می‌گوید کدام حدسش نزدیک‌تر بوده. هجده تست دارد که بیشترشان
  برای همین ردکردن‌هاست.

## پاس هشتم: هشت چیزی که مالک گزارش کرد و هیچ‌کدام «کار نمی‌کرد»

این‌ها از یک فروشگاه در حال کار گزارش شدند، نه از خواندن کد. هر هشت مورد بررسی
شد و به **ده اصلاح** رسید — نه‌تای اولش باگ واقعی بود و دهمی پیکربندی‌ای که پنل
درباره‌اش سکوت کرده بود. یک الگو در بیشترشان تکرار می‌شود و ارزش نوشتن دارد:
**فرمی که چیزی می‌فرستد که سرور نمی‌تواند بخواند، یا کنترلی که سرور از اول
قبولش نمی‌کرده** — هیچ‌کدام در تست دیده نمی‌شد، چون هر دو سرِ سیم، جدا از هم،
درست بودند.

هیچ‌کدام از این‌ها هم با «تست بیشتر» پیدا نمی‌شد؛ با **اجرا کردن خود پنل** پیدا
شد. دو تا از ده‌تا (فرم افزودن اپراتور که با اطلاعات نفر قبلی پر می‌آمد، و
همین‌ها) دقیقاً موقع باز کردن صفحه در مرورگر بیرون افتادند.

1. [x] **خروجی گرفتن از گزارش‌ها** — هرگز کار نکرده بود. دو جعبه‌ی تاریخ متن ساده
   بودند و placeholder شان تاریخ **شمسی** می‌خواست، ولی API آن فیلد را
   `DateTimeOffset?` می‌گیرد: `۱۴۰۵/۰۵/۰۱` را نمی‌شود خواند و **رشته‌ی خالی** را
   هم نمی‌شود — یعنی خالی گذاشتنشان هم همان‌قدر رد می‌شد. درخواست موقع binding
   می‌مرد، قبل از اینکه هیچ handler ای ببیندش، پس پنل چیزی جز جمله‌ی عمومی
   «ذخیره اطلاعات انجام نشد» برای گفتن نداشت. حالا `JalaliDateInput` است، تاریخ
   خالی اصلاً فرستاده نمی‌شود، و روز انتخاب‌شده به لحظه‌ی درستش در `+03:30`
   تبدیل می‌شود (تا آخرین روز بازه از گزارش نیفتد).

   دو چیز دیگر سر راه پیدا شد: فهرست گزارش‌ها **دو نام غیرواقعی** داشت
   (`products` و `finance`، در حالی که کاتالوگ `inventory` و `financial` دارد) و
   `campaigns` را که واقعاً وجود دارد نمی‌آورد؛ و بخش «ستون‌ها» شش چک‌باکس تیک‌خورده
   بود که **هیچ‌جا فرستاده نمی‌شد** — هر ترکیبی همان فایل را می‌ساخت. کنترلی که
   روی نتیجه اثر ندارد از نبودنش بدتر است: کسی تیک «مبلغ» را برمی‌دارد و فایل را
   دست حسابدار می‌دهد.

2. [x] **ثبت آدرس جدید** — هم هرگز کار نکرده بود، به **دو** دلیل مستقل.
   `SaveAddressValidator` عنوان آدرس را الزامی می‌داند و **فرم اصلاً فیلد عنوان
   نداشت** — پس هر ذخیره‌ای قبل از رسیدن به آدرس رد می‌شد، در حالی که فهرست
   آدرس‌ها «ویرایش آدرس {title}» را رندر می‌کرد. و تیک «آدرس پیش‌فرض» رشته‌ی `"on"`
   می‌فرستاد (چون از `FormData` خوانده می‌شد) به فیلدی که `bool?` است، که همان
   شکست binding است. یعنی: بدون تیک، خطای اعتبارسنجی؛ با تیک، خطای binding.

   ضمناً **پلاک، واحد و توضیحات** سه فیلدی بودند که هیچ‌جا ذخیره نمی‌شدند —
   proxy فقط فیلدهای اعلام‌شده را رد می‌کند و این سه در فهرست نبودند. مشتری پلاک
   می‌نوشت و پیک آدرسی بدون پلاک می‌گرفت. حالا داخل خود «آدرس کامل» خواسته
   می‌شوند.

3. [x] **مقاله‌ی جدید در سایت دیده نمی‌شد** — و علتش همان چیزی است که «موجودی
   اضافه کردم ولی هنوز ناموجود است» را هم توضیح می‌دهد. هر fetch در فروشگاه یک
   `tag` اعلام می‌کند و یک پنجره‌ی `revalidate` دارد (پنج دقیقه برای کاتالوگ، **یک
   ساعت** برای مجله و درخت دسته‌بندی) و **هیچ‌جای هیچ‌کدام از دو اپ، حتی یک بار،
   `revalidateTag` صدا زده نشده بود**. تگ‌ها تزئین بودند و تنها چیزی که کش را پاک
   می‌کرد گذشت زمان بود.

   حالا فروشگاه یک `POST /api/revalidate` دارد که با همان `API_KEY` ای احراز
   می‌شود که هر دو کانتینر از قبل دارند، و proxy نوشتن پنل بعد از هر ذخیره‌ی
   موفق تگ‌های مربوط را می‌اندازد. جدا بودن دو اپ یعنی این کار **باید** یک
   درخواست باشد: `revalidateTag` فقط به کش همان پروسه‌ای می‌رسد که اجرایش کرده.

4. [x] **تصویر کاور را نمی‌شد ست کرد** — و این یکی ساختاری بود، نه فقط مال مقاله.
   `IsStorableImage` فقط نشانی‌ای را می‌پذیرد که **خود همین API** از اندپوینت
   آپلودش صادر کرده باشد (همان base، همان پوشه، و یک نام ۳۲ نویسه‌ای تولیدشده) و
   بقیه را با `invalid-request: cover` رد می‌کند. ولی فرم‌های `EntityForm` یک
   جعبه‌ی متن با برچسب «نشانی تصویر» می‌دادند: **هر چیزی که در آن تایپ می‌شد طبق
   تعریف رد می‌شد** و هیچ کنترلی در آن صفحه‌ها نبود که بتواند مقداری بسازد که
   پذیرفته شود. رد کردن درست است — فروشگاهی که به عکس روی سرور دیگری لینک بدهد،
   روزی که آن سرور نظرش عوض شود عکس‌های شکسته نشان می‌دهد — چیزی که کم بود نیمه‌ی
   دیگرش بود. حالا `kind: 'image'` هست: فایل آپلود می‌شود و نشانی برگشتی همان
   چیزی است که ذخیره می‌شود. برای برند، کالکشن و دسته‌بندی هم همین‌طور.

5. [x] **حذف مقاله و کمپین** — مقاله از قبل روی API قابل بایگانی بود
   (`status: 'archived'` نرم‌حذف می‌کند) و پنل این گزینه را نمی‌داد. کمپین حتی روی
   API هم نبود: `Campaign` از روز اول `SoftDeletableEntity` بوده و **هیچ‌وقت هیچ
   متدی رویش صدا زده نشده بود** — سه وضعیتش می‌گویند کِی اجرا می‌شود، نه اینکه
   اصلاً باید باشد یا نه. حالا هر دو دکمه‌ی حذف دارند (با تأیید دوم) و
   `SaveCampaignAsync` مثل محتوا `archived` را می‌پذیرد.

6. [x] **ویرایش کمپین از فهرست باز نمی‌شد** — صفحه‌ی `/campaigns/[id]` از قبل
   وجود داشت و **جدول هیچ ستون عملیاتی نداشت**. یعنی از داخل پنل، کمپین ساخته
   می‌شد و دیگر هیچ‌وقت باز نمی‌شد مگر اینکه کسی id را حفظ باشد و آدرس را تایپ کند.
   همان اشکالی که هشت صفحه‌ی تنظیمات در پاس سوم داشتند.

7. [x] **«کاربرها از کمپین باخبر نمی‌شوند»** — این یکی باگ نبود ولی سکوت بود.
   ساخت کمپین عمداً به کسی خبر نمی‌دهد (یکی برگشت‌پذیر است، آن یکی توی جیب همه
   می‌افتد) و صفحه‌ی «ارسال اعلان» جداست و در سایدبار هم هست — ولی هیچ‌چیز در
   صفحه‌ی کمپین‌ها نمی‌گفت قدم دوم وجود دارد. حالا می‌گوید و لینک می‌دهد.

8. [x] **لاگ سرور خالی بود** — sink و خواننده وقتی چیزی پیکربندی نشده باشد **دو
   پوشه‌ی متفاوت** را نام می‌بردند: `Program.cs` به sink مسیر
   `AppContext.BaseDirectory/logs` را می‌داد و آپشن خواننده پیش‌فرض `"logs"` بود
   که نسبت به **پوشه‌ی کاری پروسه** حل می‌شود. پس روی هر هاستی که
   `Logs:Directory` را ست نکرده باشد، برنامه لاگ می‌نوشت و پنل جای دیگری دنبالش
   می‌گشت و می‌گفت «فایلی برای خواندن نیست». حالا یک ثابت مشترک است. (روی
   docker-compose این متغیر ست است، برای همین آنجا هرگز دیده نمی‌شد.)

9. [x] **پشتیبان‌گیری «ناموفق» بدون هیچ دلیلی** — دلیلش از همان اول ذخیره
   می‌شد و API آن را در فیلد `error` برمی‌گرداند؛ **جدول پنل هیچ‌وقت آن را رسم
   نمی‌کرد**. مالک یک کلمه‌ی «ناموفق» می‌دید و راهی نداشت بفهمد `pg_dump` نیست،
   دیسک پر است، یا دامپ timeout شده — سه خطا با سه جواب متفاوت، که همه یک‌شکل
   نمایش داده می‌شدند. حالا دلیل زیر نشان وضعیت نوشته می‌شود.

10. [x] **هیچ ایمیلی ارسال نمی‌شد** — این یکی **باگ کد نبود**: صندوق پستی در
    «تنظیمات ← صندوق پستی» تنظیم نشده، و `SmtpEmailSender` وقتی حساب خاموش یا
    نیمه‌کاره باشد به `ConsoleEmailSender` برمی‌گردد. آن fallback درست است
    (فروشگاهی که هنوز راه‌اندازی نشده باید بتواند سفارش بگیرد)، ولی **دیده
    نمی‌شد**: تأیید سفارش، بازیابی گذرواژه و لینک خروجی گزارش همه در لاگ نوشته
    می‌شدند و هر صفحه‌ای می‌گفت موفق بود. حالا صفحه‌ی «وضعیت سرویس‌ها» یک ردیف
    `email` دارد که degraded می‌شود و **می‌گوید کدام فیلد پر نشده** — و شرطش
    دقیقاً همان شرطی است که خود فرستنده استفاده می‌کند، در یک جا، تا این دو از هم
    فاصله نگیرند.

**یک باگی که تست خودش پیدا کرد و نزدیک بود از چشم بیفتد:** Npgsql نوع
`DateTimeOffset` را روی `timestamp with time zone` می‌نویسد و **آفستِ غیر صفر را
رد می‌کند** — throw می‌کند، تبدیل نمی‌کند. آن throw به `DbUpdateException`
می‌رسد و این API آن را «تکراری است» جواب می‌دهد، پس اولین باری که خروجی گزارش با
آفست تهران فرستاده شد، ۴۰۹ گرفت و پنل گفت مقدار از قبل ثبت شده. تا امروز دیده
نشده بود چون هرچه تاریخ می‌فرستاد `Z` می‌فرستاد
(`new Date(v).toISOString()`). حالا یک converter در لایه‌ی JSON هر لحظه‌ی ورودی
را به UTC می‌آورد — یک choke point، به‌جای اینکه هر نویسنده یادش بماند.

## پاس نهم: هفت موردی که باز هم خراب بود — و این‌بار روی داکر واقعی تست شد

پاس قبل روی تست و تایپ‌چک بسته شد و دو موردش **همچنان روی سرور خراب بود**. تفاوت
این پاس یک چیز است: کل استک با `docker compose` بالا آمد و هر مورد روی همان اجرا
آزمایش شد. سه باگ از هفت‌تا فقط با همین کار پیدا شد و با هیچ تستی پیدا نمی‌شد،
چون هیچ‌کدام در کد نبودند — در **ایمیج** و در **مقدار پیش‌فرض یک فرم** بودند.

1. [x] **پشتیبان‌گیری: خطای نسخه‌ی pg_dump.** پیامی که پاس قبل روی صفحه نشاندیم
   دقیقاً گفت مشکل چیست:
   `server version: 17.10; pg_dump version: 16.14`. کامپوز
   `postgres:17-alpine` را اجرا می‌کند و ایمیج API بسته‌ی پیش‌فرض دبیان
   (`postgresql-client` = ۱۶) را نصب می‌کرد، و pg_dump از دامپ گرفتن از سروری
   **جدیدتر از خودش** صراحتاً امتناع می‌کند. یعنی هر پشتیبانی که این نصب تا امروز
   گرفته، در همان قدم اول شکست خورده بود. حالا کلاینت از مخزن رسمی PostgreSQL و
   **هم‌نسخه‌ی سرور** نصب می‌شود.
   **آزمایش واقعی:** یک پشتیبان `full` گرفته شد → `completed`، ۱۷۰ کیلوبایت،
   و فایل دانلود و باز شد: یک zip با `database.dump` که با `PGDMP` شروع می‌شود.

2. [x] **لاگ سرور: پوشه‌ی لاگ مال root بود.** این یکی یک خط جا افتاده در
   Dockerfile بود. کانتینر با کاربر `bojan` اجرا می‌شود و ایمیج
   `/data/uploads` و `/data/backup-archives` را می‌ساخت و chown می‌کرد — ولی
   `/data/logs` را نه، در حالی که کامپوز روی همان‌جا volume می‌بندد. داکر volume
   را **مال root** می‌سازد، sink فایلیِ Serilog نمی‌تواند بنویسد، و **خطا را هم
   جایی بلند نمی‌کند** (sink ای که throw کند برنامه را سر یک خط لاگ می‌خواباند).
   نتیجه: نصبی که هیچ‌وقت روی دیسک لاگ ننوشت و پنلی که درست گزارش می‌داد فایلی
   نیست. کامنت بالای همان خط دقیقاً همین شکست را توضیح می‌داد؛ فقط اسم پوشه در
   فهرست نبود.
   **آزمایش واقعی:** بعد از بالا آمدن، `bojan-20260813.log` ساخته شد و صفحه‌ی
   «لاگ سرور» ۱۱۰ خط را نشان داد.

3. [x] **«همه‌ی تحرکات باید لاگ شود.»** `UseSerilogRequestLogging` بود ولی فقط
   متد و مسیر و وضعیت را می‌نوشت — یعنی *چه* اتفاقی افتاد و نه *برای که*. حالا هر
   خط اپراتور یا مشتری را هم می‌نویسد:
   `POST /api/admin/backups → 200 in 91.5ms by admin/03ebc61c-… from …`.
   بدنه، کوئری‌استرینگ و هدر عمداً لاگ نمی‌شوند — بدنه‌ی این API گذرواژه و توکن
   حمل می‌کند و لاگ آخرین جایی است که باید بروند.
   **یک باگ خودِ همین تغییر، که فقط با اجرا پیدا شد:** Serilog جای‌نگهدارِ
   بی‌مقدار را عیناً چاپ می‌کند، پس خط‌های ناشناس `by {Scope}/{ActorId}` می‌شدند.
   حالا همیشه مقدار می‌گیرند (`anonymous/-`).

4. [x] **خروجی گزارش: قالب پیش‌فرض قالبی بود که ساخته نمی‌شد.** worker فقط CSV
   می‌سازد و برای دو تای دیگر `NotSupportedException` می‌دهد، job را Failed
   می‌کند و دلیل را جایی ذخیره می‌کند که هیچ صفحه‌ای نمی‌خواندش — و انتخاب‌گر
   قالب هر سه را نشان می‌داد و **روی Excel شروع می‌کرد**. یعنی استفاده‌ی معمولی از
   آن صفحه (گزارش را انتخاب کن، دکمه را بزن) job ای می‌ساخت که فقط می‌توانست شکست
   بخورد، و «در صف قرار گرفت» می‌گفت. حالا API قالب نساختنی را **همان لحظه** رد
   می‌کند و پنل فقط CSV را فعال نشان می‌دهد و درباره‌ی آن دوتا می‌گوید چرا نیستند.

5. [x] **خروجی گزارش: «لینک ایمیل می‌شود» دروغ بود.** هیچ‌جای worker ایمیلی
   نمی‌فرستد، ولی `GET /reports/export/{id}/download` از روز اول وجود داشته و پنل
   هرگز صدایش نزده بود. حالا id برمی‌گردد و کنار فرم دکمه‌ی دانلود می‌آید.
   **آزمایش واقعی:** از خود پنل خروجی گرفته شد و فایل CSV با ۲۰۰ دانلود شد.

6. [x] **صندوق پستی: ۵۰۲ به‌جای دلیلش.** API جمله‌ی درست را در `detail` می‌فرستاد
   («صندوق ورودی پیکربندی یا فعال نشده است…») و لایه‌ی کلاینت پنل به‌جای آن پیام
   عمومی خودش را نشان می‌داد — تابعی که کامنتش می‌گفت جمله‌ی API را برمی‌دارد،
   `ApiError.message` را برمی‌داشت. حالا `detail` را می‌خواند.

7. [x] **پاسخ‌های آماده در صفحه‌ی چت نبودند.** روی تیکت بودند و روی چت زنده نه —
   یعنی دقیقاً همان‌جایی که مشتری پشت خط منتظر است و پاسخ آماده بیشترین ارزش را
   دارد. حالا در هر دو هست.

8. [x] **باشگاه مشتریان: چرا برای مشتری دیده نمی‌شد.** باگ نبود؛ `Enabled` یعنی
   «حداقل یک پله تعریف شده»، و بدون پله صفحه‌ی `/loyalty` عمداً ۴۰۴ می‌دهد و لینک
   فوتر هم نمی‌آید — تا فروشگاه چیزی را تبلیغ نکند که تعریفش نکرده. چیزی که نبود،
   **گفتنش به مالک** بود. حالا صفحه‌ی تنظیمات باشگاه بالای فرم می‌گوید باشگاه
   همین حالا برای مشتری‌ها دیده می‌شود یا نه، و چه چیزی دیده‌شدنش را روشن می‌کند.

9. [x] **سفارش: «تحویل شده» به «مرجوع شده» تغییر نمی‌کرد.** `Delivered` روی مسیر
   ارسال حالت نهایی است، پس دامنه حرکت را رد می‌کرد و API ۴۰۹ `terminal-status`
   می‌داد — کلیدی که پنل برایش جمله‌ای نداشت، پس «این مقدار از قبل ثبت شده است»
   نشان می‌داد و مثل دکمه‌ای به‌نظر می‌رسید که کاری نمی‌کند. **مجاز کردن آن حرکت
   اصلاحِ بدتری بود**: مرجوعی پول برمی‌گرداند و ممکن است کالا را به انبار بیاورد،
   و تغییر وضعیت هیچ‌کدام را نمی‌کند — همان استدلالی که «لغو شده» را از این کنترل
   بیرون نگه داشته. حالا با کلید `use-returns-screen` رد می‌شود و خود کنترل به
   صفحه‌ی «مرجوعی‌ها» لینک می‌دهد.

**آنچه هنوز باز است و کد حلش نمی‌کند:** تا وقتی «تنظیمات ← صندوق پستی» پر نشود
هیچ ایمیلی ارسال نمی‌شود. این از پاس قبل روی صفحه‌ی «وضعیت سرویس‌ها» با ردیف
`email` اعلام می‌شود و در همین اجرا هم در لاگ دیده شد.

## پاس دهم: مقاله‌ای که در جدول اشتباه ذخیره می‌شد

پنل یک ویرایشگر مقاله داشت که **در جدولی می‌نوشت که هیچ‌جای سایت آن را
نمی‌خواند**. این ریشه‌ی دو گزارش جداست و تا وقتی دیده نشود، هر دو مثل باگ‌های
بی‌ربط به‌نظر می‌رسند.

1. [x] **«مقاله‌ای که منتشر کردم در مجله دیده نمی‌شود» و «مجله مقاله‌هایی دارد که
   در پنل نیستند» — یک علت.** «مدیریت محتوا» یک `ContentEntry` با
   `kind: article` می‌سازد؛ مجله جدول `Article` را می‌خواند — جدولی جدا با
   ستون‌های خودش: دسته‌ی تحریریه، زمان خواندن، پرچم «ویژه» و بدنه‌ای از بلوک‌های
   نوع‌دار که صفحه‌ی مقاله رندر می‌کند. هیچ رابطه‌ای بین این دو نبود. پس مقاله‌ی
   منتشرشده از پنل هیچ‌جا نمی‌رفت، و هشت مقاله‌ی روی سایت (که seeder در جدول
   درست نوشته بود) در پنل دیده نمی‌شدند.

   حالا پنل روی `Article` می‌نویسد: `GET/POST /admin/articles`، فهرست و
   ویرایشگر جدید، و بایگانی به‌جای حذف. `ContentEntry` برای همان سه چیزی می‌ماند
   که برایشان درست بود — صفحه‌ی ثابت، بنر، پرسش متداول.

   **بدنه** متن ساده است و سرویس ترجمه‌اش می‌کند: خط خالی بین پاراگراف‌ها، و خطی
   که با `##` شروع شود تیتر. زمان خواندن هم **فیلد نیست** — از روی متن حساب
   می‌شود، چون واقعیتی درباره‌ی متن است نه تصمیمی که باید دستی هماهنگ بماند.

   **آزمایش واقعی:** روی داکر، یک مقاله از API پنل منتشر شد و بلافاصله در
   `GET /api/articles` ظاهر شد — با slug فارسی، دسته، و زمان خواندن محاسبه‌شده؛
   صفحه‌ی مقاله هم بلوک‌ها را درست برگرداند (heading + دو پاراگراف). فهرست پنل
   حالا همان ۸ مقاله‌ای را نشان می‌دهد که مجله دارد.

   **یک ۵۰۰ که همین اجرا پیدا کرد:** `Slug.From` برای مقداری که هیچ حرف
   قابل‌استفاده‌ای ندارد throw می‌کند. این درست است به‌عنوان یک invariant دامنه و
   غلط است به‌عنوان پاسخ به فرمی که کسی پرش می‌کند — بدون گرفتنش، یک عنوان
   نامناسب ۵۰۰ می‌داد. حالا خطای فیلد است. (فارسی خودش کاملاً slug می‌شود؛
   generator اسکریپت را نگه می‌دارد و رومانیزه نمی‌کند.)

2. [x] **فهرست محتوا ستون «عملیات» نداشت.** ویرایشگرهای هر نوع وجود داشتند و
   هیچ‌چیز به آن‌ها لینک نمی‌داد، پس از این صفحه هیچ ردیفی باز نمی‌شد. حالا هر
   ردیف به ویرایشگر نوع خودش می‌رود، و حذف (بایگانی) روی ویرایشگرهای صفحه، بنر و
   پرسش متداول هم اضافه شد.

3. [x] **سبد خرید بعد از خرید خالی نمی‌شد.** `clear()` صدا زده می‌شد ولی
   `localStorage` را یک `useEffect` می‌نوشت، و خط بعدیِ `PlaceOrderButton`
   `window.location.href = paymentUrl` بود — ناوبری کامل، همان لحظه. dispatch
   هنوز رندر نشده بود، پس effect هرگز اجرا نمی‌شد و مشتری از درگاه به سبدی
   برمی‌گشت که هرچه خریده بود هنوز در آن بود. پرداخت در محل فقط به این دلیل
   سالم بود که `router.push` ناوبری سمت کلاینت است. حالا خالی‌شدن **همان‌جا**
   در storage نوشته می‌شود.

4. [x] **عدد روی سبد خرید تعداد کل واحدها را می‌شمرد.** سه تا از یک دفتر،
   عدد ۳ — که خوانده می‌شود «سه چیز در سبد» و سبد یک چیز دارد. حالا تعداد
   ردیف‌هاست.

5. [x] **خروجی Excel واقعاً ساخته می‌شود.** یک نویسنده‌ی xlsx دستی، بدون
   وابستگی جدید: یک xlsx زیپی از چند بخش XML است و زیرمجموعه‌ی لازم برای یک شیت
   ساده کوچک است، و `System.IO.Compression` از قبل اینجاست (worker پشتیبان‌گیری
   با همان آرشیو می‌سازد). عددها عدد می‌مانند — همان دلیلی که کسی به‌جای CSV
   spreadsheet می‌خواهد — متن فارسی سالم می‌ماند، و همان محافظت ضد فرمول CSV
   اینجا هم هست (اینجا مهم‌تر است: این فایل *خودش* spreadsheet است).
   هفت تست روی خود فایل، از جمله ارجاع ستون‌ها بعد از Z و فرار از XML.

   **PDF عمداً باز مانده.** یک PDF باید فونت را جاسازی کند و متن فارسی را
   راست‌به‌چپ shape کند؛ نویسنده‌ای که این دو را نکند صفحه‌ای از حروف جدا و
   برعکس تولید می‌کند — که از نبودنش بدتر است، چون شبیه گزارش به‌نظر می‌رسد. صف
   این قالب را همان اول رد می‌کند و خود دکمه می‌گوید چرا.

**پاسخ به سؤال پشتیبان‌گیری:** نوع `full` **هر دو** را می‌گیرد — دامپ کامل
دیتابیس (`database.dump`، فرمت custom که `pg_restore` می‌خواند) و کل درخت
فایل‌های آپلودشده زیر `media/`. روی همین اجرا آزمایش شد: یک فایل در
`/data/uploads/content` گذاشته شد و در آرشیو به‌صورت
`media/content/deadbeef.jpg` کنار `database.dump` ظاهر شد. نوع‌های `database` و
`media` هم جدا وجود دارند اگر فقط یکی را بخواهید.

## پاس یازدهم: تقویم شمسی، تصویرها، مشتری کامل، و گزارش ممیزی

1. [x] **همه‌ی تقویم‌های پروژه شمسی شدند.** پنج جای باقی‌مانده هنوز
   `<input type="date">` بودند — کمپین‌ها و هر فیلد تاریخ `EntityForm`، کد
   تخفیف، تخفیف محصول، پیش‌فاکتور سازمانی، و تاریخ واریز کارت‌به‌کارت در
   فروشگاه. `type="date"` مقدار درست را post می‌کند و **تقویم میلادی می‌کشد**:
   کمپینی که تا ۲۹ مرداد است را کسی تنظیم می‌کرد که August می‌خواند.
   `JalaliDateInput` حالا حالت controlled هم دارد (سه صفحه بازه را حین انتخاب
   اعتبارسنجی می‌کنند و مقدار را در state نگه می‌دارند)، و `name` اختیاری شد چون
   caller ای که مقدار را خودش نگه می‌دارد چیزی برای post کردن ندارد.

2. [x] **بارگذاری تصویر برای برند، کالکشن و بنر.** همان اشکالی که مقاله داشت:
   جعبه‌ی متن «نشانی تصویر» در برابر APIای که فقط نشانی صادرشده‌ی خودش را
   می‌پذیرد — یعنی هیچ مقدار قابل قبولی وجود نداشت. حالا همه‌جا `ImageField`
   است.

3. [x] **مدیریت کامل مشتری.** `Customer.Code` اضافه شد — شناسه‌ی کوتاه فروشگاه
   (`BZ-00042`) که روی سفارش و پشت تلفن به آن ارجاع می‌دهید، برخلاف GUID که
   خواندنی نیست و شماره‌ی موبایل که داده‌ی شخصی است. مهاجرت برای ردیف‌های
   موجود کد تولید می‌کند **قبل از** ساختن ایندکس یکتا (وگرنه روی هر دیتابیسی که
   بیش از یک مشتری دارد می‌شکست)، و شماره‌ی بعدی از یک sequence می‌آید نه از
   `MAX+1` — دو ثبت‌نام هم‌زمان همان عدد را می‌خواندند و یکی‌شان شکست می‌خورد.

   کارت «اطلاعات مشتری» حالا همه‌ی فیلدهای رکورد را ویرایش می‌کند و حساب بدون
   سابقه را کامل حذف می‌کند. **موجودی کیف پول و امتیاز باشگاه عمداً نیستند**:
   این‌ها دفتر حساب‌اند و با تراکنشی حرکت می‌کنند که دلیلش را ثبت می‌کند؛ فیلدی
   که مستقیم بنویسدشان راهی است برای شارژ حساب بدون هیچ ردی.

   حذف برای حسابی که سفارش، تراکنش کیف پول یا تیکت پشتیبانی دارد **رد می‌شود** —
   فاکتوری که نام مشتری‌اش پاک شده فاکتور مرتب‌تری نیست. صفحه این را می‌گوید و
   به تعلیق ارجاع می‌دهد.

### گزارش ممیزی

از ۲۱ مورد، **هرکدام جداگانه روی کد بررسی شد** نه اینکه فرض شود درست است:

- **C-1 (ثبت دوباره‌ی فایل استاتیک) — واقعی بود.** دو `UseStaticFiles` روی یک
  پوشه، با دو ریزالو متفاوت: یکی نسبت به working directory، یکی نسبت به
  `ContentRootPath`. با مسیر مطلق (که compose می‌دهد) یکی می‌شوند و با مسیر
  نسبی دو پوشه‌ی مختلف. یک mount ماند — همانی که `nosniff` و
  `Content-Disposition: inline` هم می‌فرستد.

  **کدام base درست است را تست‌ها مشخص کردند، نه حدس من.** اول ریزالو را به
  `ContentRootPath` بردم چون «درست‌تر» به‌نظر می‌رسید و سه تست مدیا شکست: نویسنده
  (`LocalFileStorage`) با `Path.GetFullPath` خالی ریزالو می‌کند، یعنی نسبت به
  working directory. این انتخاب بین دو base نیست — نویسنده یکی را برداشته و
  خواننده‌ای که دیگری را بردارد پوشه‌ی خالی سرو می‌کند در حالی که فایل‌ها همان‌جا
  که گذاشته شده‌اند نشسته‌اند.
- **L-6 (آدرس IPv4-mapped) — واقعی بود**، و در لاگ همین استقرار دیده می‌شود
  (`::ffff:172.20.0.1`). یک کلاینت دو partition و دو پنجره می‌گرفت، یعنی دو
  برابر سقف. حالا نرمال می‌شود.
- **L-5 (نبود `Retry-After`) — واقعی بود.** فرانت این هدر را می‌خواند و جمله‌ی
  «چند ثانیه بعد» را از رویش می‌سازد؛ فقط هیچ‌وقت دریافتش نمی‌کرد.
- **H-4 (captive dependency در SMS) — نادرست بود.** `ConfiguredSmsSender`
  اصلاً سرویس scoped نمی‌گیرد؛ `IServiceScopeFactory` می‌گیرد و هر بار scope
  می‌سازد. گزارش هم خودش نوشته «احتمالاً».
- **M-1 (کش بدون `Size`) — نادرست بود.** `entry.Size = 1` سر جایش هست، با
  کامنتی که دقیقاً همین را توضیح می‌دهد.

بقیه‌ی موارد یا رفتار عمدی و مستندند (H-3 و M-3 و L-2 در README و همین فایل
توضیح داده شده‌اند)، یا در حد نظر معماری‌اند (M-6: دو نسخه‌ی session)، که هیچ‌کدام
تغییر کورکورانه نمی‌خواهند.

### آنچه در این پاس انجام **نشد** و چرا

- **یکی‌شدن حساب ادمین و مشتری.** این تغییر نام نیست: `AdminUser` کلید خارجی
  ردیف‌های ممیزی، کلیدهای API، حرکت‌های انبار و پیش‌فاکتورهاست و `Customer` کلید
  خارجی هر سفارش. یکی کردنشان یعنی مهاجرتی که هر دو دسته ارجاع را جابه‌جا کند،
  و نیمه‌کاره انجام دادنش بدتر از انجام ندادنش است. در roadmap ریدمی نوشته شد.
- **«ارسال اعلان عین فونیکس».** صفحه‌ی ارسال اعلان (اعلان درون‌برنامه‌ای، پیامک،
  نوتیفیکیشن مرورگر، انتخاب مخاطب) از قبل هست و کار می‌کند؛ اینکه دقیقاً کدام
  تفاوت با فونیکس مدنظر است از گزارش روشن نشد. مشخص کنید تا همان ساخته شود.

## پاس دوازدهم: یک هویت، و یک فهرست کاربران

1. [x] **ادمین حالا در سایت اصلی هم لاگین می‌کند و خرید می‌کند.** اعتبارنامه‌ی
   اپراتور فقط پنل را باز می‌کرد، پس همان آدم برای خرید به حساب دوم با گذرواژه‌ی
   دوم نیاز داشت — و فروشگاه هیچ راهی نداشت بفهمد این دو یک نفرند.

   **پیوند، نه ادغام.** `AdminUser.CustomerId` اضافه شد. دو جدول کلیدهای خارجی
   متفاوتی دارند — این ردیف را ردیف‌های ممیزی و کلیدهای API و حرکت‌های انبار
   نام می‌برند، و `Customer` را هر سفارش — پس یکی کردنشان در یک جدول یعنی
   جابه‌جا کردن هر دو دسته ارجاع. پیوند یعنی **یک نفر، یک گذرواژه**، و هر جدول
   همان ارجاع‌هایی را نگه می‌دارد که برایشان ساخته شده.

   گذرواژه **یک جا** می‌ماند: ورود در فروشگاه در برابر رکورد اپراتور بررسی
   می‌شود و بعد به‌عنوان حساب خرید پیوندخورده‌اش وارد می‌کند — پس هش دومی برای
   هماهنگ نگه‌داشتن وجود ندارد و لحظه‌ای نیست که این دو درباره‌ی گذرواژه با هم
   اختلاف پیدا کنند.

   حساب خرید در **اولین ورود** ساخته می‌شود نه هنگام تعریف اپراتور، چون بیشتر
   اپراتورها هرگز خرید نمی‌کنند و ردیفی که کسی نخواسته فقط فهرست مشتری‌ها را
   گیج می‌کند. اگر همان شماره از قبل مشتری باشد، **پیوند می‌خورد نه تکرار** — و
   نام و ایمیل آن مشتری دست‌نخورده می‌ماند، چون نوشتن روی آن یعنی ویرایش حساب
   کس دیگری.

   اپراتور معلق در فروشگاه هم وارد نمی‌شود — وگرنه تعلیق در پنل قفل نبود، در
   بود. و اپراتوری که شماره‌ی موبایل ندارد پیام روشن می‌گیرد که کدام فیلد کم
   است، نه «گذرواژه اشتباه است»: مشتری یعنی یک شماره‌ی موبایل، و بدون آن چیزی
   برای ساختن حساب خرید نیست.

   هشت تست، و روی داکر واقعی هم آزمایش شد: ابتدا `operator-needs-phone` گرفت،
   بعد از ثبت شماره با همان گذرواژه‌ی پنل در فروشگاه وارد شد.

2. [x] **یک فهرست کاربران، عین فونیکس.** «مشتریان» و «کاربران ادمین» دو صفحه
   بودند، یعنی «چه کسانی اینجا حساب دارند؟» دو جواب داشت و هیچ نشانه‌ای نبود که
   اپراتور یک فهرست و مشتری فهرست دیگر یک نفرند. حالا `GET /admin/accounts` هر
   دو را با **نقش** برمی‌گرداند (`customer` یا نقش اپراتور)، صفحه‌ی «کاربران»
   کنار هم نشانشان می‌دهد، فیلتر نقش دارد، و هر ردیف به ویرایشگر خودش می‌رود.
   اپراتوری که خرید هم می‌کند نشان «خریدار هم هست» می‌گیرد.

   دو کوئری و یک merge، نه UNION در SQL: دو جدول تقریباً هیچ ستون مشترکی ندارند
   و union دو projection که باید همیشه یکسان بمانند، دفعه‌ی بعد که یکی فیلد
   بگیرد بی‌صدا می‌شکند.

3. [x] **ارسال پیام از ویرایش کاربر برداشته شد و به «ارسال اعلان» رفت** — عین
   فونیکس. یک کار بود که بین دو صفحه پخش شده بود، و آن صفحه‌ای که کسی سراغش
   نمی‌رفت دقیقاً همانی بود که موقع «می‌خواهم به این مشتری چیزی بگویم» لازم
   می‌شد. صفحه‌ی مشتری حالا لینک می‌دهد و آن مشتری از قبل انتخاب‌شده باز می‌شود.

**بخش افزودن ادمین دست نخورد**، طبق خواسته.

## پاس سیزدهم: بقیه‌ی گزارش ممیزی

پاس قبل موارد بحرانی و بلندمرتبه‌ی گزارش را برداشت. این پاس بقیه را برداشت —
هر کدام اول در کد بررسی شد و بعد تغییر کرد، چون در پاس قبل چهار مورد گزارش
اصلاً واقعی نبودند و «رفع» کردنشان یعنی خراب کردن چیزی که کار می‌کرد.

1. [x] **C-2 (بحرانی) — رقابت روی کد یک‌بارمصرف.** ساخت چالش OTP یک delete بود
   بعد یک insert: دو درخواست هم‌زمان برای یک شماره هر کدام آنچه را دیده بودند
   حذف می‌کردند و هر کدام یک ردیف می‌افزودند، و شماره با **دو چالش زنده** باقی
   می‌ماند. کد این را با «جدیدترین را بردار» دور می‌زد، که کار می‌کرد و ردیف
   بازنده را برای همیشه در جدول جا می‌گذاشت.

   حالا ایندکس یکتا روی شماره + یک `INSERT ... ON CONFLICT DO UPDATE`: رقابت
   در پایگاه داده حل می‌شود نه در حافظه‌ی برنامه. مهاجرت پیش از ساختن ایندکس
   ردیف‌های تکراری موجود را پاک می‌کند، وگرنه روی داده‌ی امروز اجرا نمی‌شد.

   `Attempts` هم صفر می‌شود: چالش تازه یعنی پنج فرصت تازه، و ارث‌بردن خطاهای
   چالش قبلی یعنی «کد را دوباره بفرست» چیزی به کاربر برنمی‌گرداند.

   **مسیر پرخطر مهاجرت جدا آزمایش شد**، چون تست‌ها روی پایگاه داده‌ی خالی
   مهاجرت می‌کنند و هرگز به ردیف تکراری نمی‌خورند: روی یک Postgres موقت جدولِ
   پیش از مهاجرت ساخته شد با سه ردیف برای یک شماره و دو ردیف با انقضای یکسان.
   ساختن ایندکس یکتا بدون پاک‌سازی همان‌طور که انتظار می‌رفت شکست
   (`Key ("Phone")=(09120000001) is duplicated`)؛ با پاک‌سازی، جدیدترین ردیف هر
   شماره ماند، تساوی با شناسه‌ی بزرگ‌تر شکست، ردیف تنها دست نخورد، و upsert
   روی همان جدول هم جایگزینی کرد و هم شمارنده‌ی تلاش را صفر کرد.

2. [x] **H-1 — مقایسه‌ی کلید API با زمان ثابت.** کلید ارائه‌شده و کلید پیکربندی
   بایت‌به‌بایت با `FixedTimeEquals` مقایسه می‌شدند، اما این تابع برای طول‌های
   نابرابر زود برمی‌گردد — یعنی **طول** کلید از روی زمان پاسخ قابل حدس بود. حالا
   هر دو اول SHA-256 می‌شوند و بعد مقایسه: دو ورودی همیشه ۳۲ بایت‌اند، پس طول
   چیزی لو نمی‌دهد.

3. [x] **H-2 — نشست بازمانده بعد از تغییر گذرواژه.** بازنشانی گذرواژه در API
   مهر امنیتی حساب را می‌چرخاند، یعنی کوکی داخل مرورگر همان لحظه مرده است — ولی
   فرم بازنشانی آن را نگه می‌داشت و کاربر به صفحه‌ای می‌رفت که با کوکی مرده
   دوباره بیرونش می‌انداخت. حالا پیش از هدایت، نشست پاک می‌شود.

4. [x] **M-4 — سقف اندازه‌ی بدنه‌ی درخواست.** Kestrel سقف پیش‌فرض ۳۰ مگابایت
   داشت در حالی که آپلود ما ۸ مگابایت است. حالا ۱۲ مگابایت (۸ به‌علاوه‌ی
   سربار multipart): درخواست بزرگ‌تر پیش از خوانده‌شدن رد می‌شود نه بعد از آنکه
   حافظه‌اش را گرفته باشد.

5. [x] **M-6 — دو نسخه‌ی کد امضای نشست، حالا یکی.** گزارش این را «نظر معماری»
   نوشته بود و پاس قبل هم همین‌طور کنارش گذاشت. اشتباه بود: دو نسخه **از قبل
   واگرا شده بودند** — فروشگاه امضا را در یک جفت `sign`/`verify` جمع کرده بود و
   پنل همان چهارده خط را در چهار تابع تکرار کرده بود. این دقیقاً همان شکلی است
   که یک اصلاح امنیتی به سه جا از چهار جا اعمال می‌شود.

   پاکت مشترک به `@bojan/config/signed-cookie` رفت. آنچه **نرفت**: نام کوکی،
   عمر نشست، شکل payload و کلید امضا — کلید دو اپ عمداً متفاوت است تا کوکی
   مشتری هرگز به‌جای کوکی اپراتور جا نزند، و کلید هر بار پرسیده می‌شود نه یک بار
   خوانده، تا خطای «AUTH_SECRET تنظیم نشده» سر درخواست بیفتد نه سر build.

   ۹ تست تازه: رفت‌وبرگشت، payload فارسی، امضا با کلید دیگر، بدنه‌ی دست‌کاری‌شده
   روی امضای معتبر، و ورودی‌هایی که اصلاً توکن نیستند.

6. [x] **M-8 — کش حالت تعمیر، بدون هجوم.** گزارش نگران thread safety بود؛ آن
   نگرانی بی‌مورد است، چون هر دو runtime تک‌نخ‌اند. مشکل واقعی جای دیگری بود:
   کش **پیش از** fetch خوانده و **پس از** آن نوشته می‌شود، و `await` یعنی
   یله‌کردن نخ — پس هر درخواستی که در فاصله‌ی این دو می‌رسید کش منقضی می‌دید و
   درخواست خودش را می‌فرستاد. یعنی بدترین حالت دقیقاً وقتی رخ می‌داد که بیشترین
   ضرر را دارد: هجوم ترافیک، یا API کند.

   حالا promiseی در جریان به اشتراک گذاشته می‌شود و پاسخ ناموفق هم کش می‌شود:
   API خوابیده هر پنج ثانیه یک بار پرسیده می‌شود، نه به ازای هر بازدیدکننده.

7. [x] **L-3 — `JsonWebTokenHandler` به‌جای `JwtSecurityTokenHandler`.** همان
   handleری که میان‌افزار bearer از قبل با آن توکن ورودی را **اعتبارسنجی**
   می‌کند، حالا همان را هم **صادر** می‌کند؛ و چون thread-safe است یک نمونه برای
   کل پروسه بس است، نه یکی به ازای هر ورود.

   یک تله سر راه بود: handler قدیمی `ClaimTypes.Role` را از طریق نگاشت خروجی‌اش
   بی‌صدا به `role` تبدیل می‌کرد و handler جدید هرچه بگیری همان را می‌نویسد —
   پس ادعا صراحتاً `role` نوشته شد. اگر این را ندیده می‌گرفتیم، توکن‌ها ادعای
   نقشی با نام URI می‌گرفتند و **هیچ اپراتوری هیچ اجازه‌ای نداشت**.

### مواردی که بررسی شدند و تغییری لازم نداشتند

- **L-1 (نبود healthcheck برای فرانت) — نادرست بود.** healthcheck در
  `frontend/Dockerfile` تعریف شده و compose آن را از خود image برمی‌دارد. آن
  نسخه از نسخه‌ای که می‌شد اینجا نوشت **بهتر** هم هست: پاسخ ۵۰۳ حالت تعمیر را
  سالم حساب می‌کند، وگرنه فروشگاهی که دقیقاً طبق تنظیم مالکش کار می‌کند به‌عنوان
  کانتینر خراب گزارش می‌شد و هر ارکستراتوری تا ابد ری‌استارتش می‌کرد.
- **M-7 (سفارش بدون قلم) — نادرست بود.** `CheckoutService.PlaceOrderAsync` از
  قبل `Lines.Count is 0` را رد می‌کند.
- **M-2 (لو رفتن کد در لاگ) — نادرست بود.** آن خط فقط برای شماره‌ی توسعه‌ی
  پیکربندی‌شده اجرا می‌شود و در Development هم فقط وقتی صراحتاً روشن باشد.
- **H-5 (نبود CORS) — عمدی است، و حالا نوشته شده.** هیچ مرورگری قرار نیست به
  این API برسد: هر دو سایت از پروسه‌ی Node خودشان و از روی شبکه‌ی داخلی صدایش
  می‌زنند و هر تماس `X-Api-Key` دارد. تنها چیزی که مرورگر مستقیم بار می‌کند
  `/media` است و تصویر در `<img>` اصلاً مشمول CORS نیست. یعنی **نبودن سیاست
  CORS همان چیزی است که جلوی صدا زدن API از یک اوریجین دیگر را می‌گیرد**؛
  افزودن سیاست بازْ برداشتن یک مرز است نه رفع یک باگ. دلیلش در `Program.cs`
  جایی که سیاست باید تعریف می‌شد نوشته شد.
- **L-4 (کوکی پنل با `SameSite=Strict`) — هزینه‌اش اینجا صفر است.** ایرادِ Strict
  آن اپراتوری است که روی لینک پنل در ایمیل کلیک می‌کند و با نشست معتبر به صفحه‌ی
  ورود می‌رسد. این فروشگاه **هیچ لینکی به پنل نمی‌فرستد** — نه اعلان، نه گزارش،
  نه هشدار. این شرط در کنار همان تنظیم نوشته شد: هر تغییری که شروع به فرستادن
  لینک عمیق پنل کند باید در همان تغییر این را `lax` کند.
- **H-3 و M-3 و L-2** همان‌طور که پاس قبل نوشت عمدی و مستندند: ثبت‌نام ناچار
  است بگوید شماره از قبل حساب دارد (فرمی که ادعا کند حسابی ساخته که نساخته
  بدتر است)، کد ثابت توسعه فقط از یک خط `IsDevelopment()` عبور می‌کند، و
  گذرواژه‌ی پایگاه داده‌ی توسعه روی هیچ میزبان واقعی خوانده نمی‌شود. برداشتن
  اوراکل ثبت‌نام یعنی تأیید شماره **پیش از** ساخت حساب — تغییری در صفحات
  ثبت‌نام، که در roadmap ریدمی نشسته و بدون درگاه پیامک فعال، فروشگاه را از ثبت
  مشتری می‌اندازد.

### روی استک واقعی هم آزمایش شد

نه فقط تست‌ها. کل استک با `docker compose` بالا آمد و:

- مهاجرت روی بوت یک استقرار واقعی اجرا شد و ایندکس یکتا سر جایش نشست.
- **سه** درخواست کد یک‌بارمصرف پشت سر هم برای یک شماره → **یک** ردیف. پیش از
  این می‌توانست سه تا باشد.
- ثبت‌نام در فروشگاه کوکی نشست را ست کرد و `/account` باز شد؛ بدون کوکی همان
  صفحه به `/login?next=/account` رفت. ورود به پنل هم همین‌طور — یعنی پاکتِ
  مشترکِ تازه در هر دو اپ و با دو کلید متفاوت کار می‌کند.
- توکن bearer روی API واقعی: مسیر مالک ۲۰۰، بدون توکن ۴۰۱، و همان توکن ادمین
  روی مسیر مشتری ۴۰۳.
- **۳۰ بارگذاری هم‌زمان صفحه‌ی اصلی → فقط ۱ تماس `store/status`** در لاگ API.
  این همان چیزی است که M-8 درباره‌اش بود.
- لاگ درخواست‌ها با هویت کنشگر همچنان نوشته می‌شود.

آنچه روی استک **آزمایش نشد**: مسیر بازنشانی گذرواژه (H-2)، چون توکنش با ایمیل
می‌رود و روی این میزبان SMTP تنظیم نیست. تغییرش سه خط است و مسیر خروج از حساب
که صدا می‌زند از قبل وجود دارد و تست دارد.

### چیزی که گزارش ندیده بود، و تست تازه پیدایش کرد

**کلید امضای JWT در سمت اعتبارسنجی، پیش از موعد خوانده می‌شد.** برای نوشتن تست
L-3 معلوم شد هیچ تستی در کل پروژه توکن bearer را **به‌عنوان** bearer ارائه
نمی‌کند — همه از هدرهای پروکسی مورد اعتماد استفاده می‌کنند، چون پنل هم همان کار
را می‌کند. یعنی نیمه‌ی JWT همان سیاست‌ها، همان نیمه‌ای که تصمیم می‌گیرد کسی که
پنل را دور زده چه اجازه‌ای دارد، در هر ورود صادر می‌شد و **هرگز یک بار هم
ارائه نمی‌شد**.

اولین تستی که ارائه‌اش کرد ۴۰۱ گرفت: `issuer` و `audience` داخل delegateی خوانده
می‌شوند که بعد از ساخت host اجرا می‌شود، اما کلید امضا در متغیری **بالای** آن
خوانده شده بود — یعنی وقتی builder هنوز در حال ساخته‌شدن است. هر منبع
پیکربندی‌ای که بعد از آن خط ثبت شود (دقیقاً کاری که میزبان تست می‌کند) نتیجه‌اش
API‌ای است که با کلید واقعی **امضا** می‌کند و با placeholderِ `0000...`
**اعتبارسنجی**. هر درخواست bearer ۴۰۱ می‌گیرد با پیام «کلید امضا پیدا نشد»، و
چون طرح پروکسی مورد اعتماد سر جایش کار می‌کند، هیچ‌کس متوجه نمی‌شود.

خواندن کلید به داخل همان delegate منتقل شد. پنج تست تازه (`BearerTokenTests`)
حالا این مسیر را می‌پوشانند: توکن مالک صفحه‌ی مالک را باز می‌کند، توکن پشتیبانی
صفحه‌ی پشتیبانی را باز می‌کند و صفحه‌ی مالک را نه، توکن ادمین در مسیرهای مشتری
رد می‌شود و برعکس، و توکنی که یک کاراکترِ امضایش عوض شده رد می‌شود.

### یک چیز که در گزارش نبود

`SSH.NET 2025.1.0` آسیب‌پذیری شناخته‌شده‌ی high دارد و هر build هشدارش را چاپ
می‌کند. در کد پروژه استفاده نشده — وابستگی غیرمستقیم پروژه‌ی تست است — پس روی
چیزی که مستقر می‌شود اثری ندارد و اینجا دست نخورد. اگر بخواهید بسته‌ی تست را
جابه‌جا کنیم، همان‌جا برداشته می‌شود.

## پاس چهاردهم: هفت مورد، و یک نگهبان که کل کش سایت را قفل کرده بود

هفت ایراد به‌صورت شماره‌دار گزارش شد. هر هفت‌تا رفع شد و **روی استک واقعی
داکر** آزمایش شد، نه فقط با تست. یکی از آن‌ها ریشه‌ای داشت که دو گزارش دیگر را
هم توضیح می‌داد.

### ۱. حذف کاربر در مدیریت کاربران نبود

مسیر `POST /admin/customers/delete` از همان پاسی که مدیریت مشتری ساخته شد وجود
داشت، در فهرست مجاز پروکسی پنل هم بود، و **هیچ صفحه‌ای صدایش نمی‌زد**. یعنی
قابلیتی که API داشت و اپراتور نداشت.

`CustomerDeletePanel` اضافه شد، پایین صفحه‌ی مشتری و فقط برای owner — همان‌جا که
بستن دسترسی هست، چون هر دو از یک جنس‌اند. دو مرحله‌ای است: حذف حساب برگشت ندارد
و دکمه‌ای که یک کلیک با «ویرایش» فاصله دارد، روزی اشتباهی زده می‌شود.

مشتری‌ای که سابقه‌ی خرید دارد را **سرور** رد می‌کند نه این صفحه، و جوابش حالا
جمله‌ی خودش را دارد: «سفارش‌هایش به نام او ثبت شده‌اند؛ به‌جای حذف دسترسی‌اش را
ببندید.» پیش از این همان رد به‌صورت «این مقدار از قبل ثبت شده است» می‌رسید که
نه می‌گفت چه چیزی و نه چه کار باید کرد.

### ۲. اعلان‌ها عین فونیکس نبود و جستجو نداشت

صفحه فقط یک فرم ارسال گروهی بود: مخاطب را از میان گروه‌های مشتری انتخاب کن و
بفرست. دو چیز که اپراتور اول از همه سراغشان می‌رود نبود — نوشتن به **یک** نفر
(که در صفحه‌ی خود مشتری بود، نه اینجا) و دیدن این‌که **چه چیزی قبلاً فرستاده
شده**. پس «قبلاً به او گفته‌ایم؟» در هیچ‌جای پنل جواب نداشت.

`NotificationCenter` جای آن نشست، با همان چیدمان فونیکس: سمت راست فرم ارسال
(گیرنده: همه‌ی کاربران یا یک کاربر، عنوان، متن، لینک) و سمت چپ فهرست
ارسال‌شده‌ها با حذف. جستجو روی **هر دو** نیمه، چون هر دو فهرست بی‌انتها رشد
می‌کنند: فروشگاهی با چهار هزار مشتری را نمی‌شود با یک dropdown اداره کرد، و یک
سال پیام را نمی‌شود با اسکرول خواند.

پشتش دو مسیر تازه: `GET /admin/notifications` که پیام عمومی و پیام تک‌نفره را از
دو جدول در یک فهرست ادغام می‌کند، و `POST /admin/notifications/delete`. حذف یک
کمپین، کپی‌هایی که برای مشتری‌ها ساخته را هم می‌برد — ردیفی که به کمپینِ
حذف‌شده اشاره کند، اعلانی است که هیچ‌کس نمی‌تواند منشأش را توضیح دهد.

انتخاب کانال (پیامک/ایمیل/مرورگر) نگه داشته شد. فونیکس ندارد، اما این فروشگاه
دارد و برداشتنش یعنی از دست دادن ارسال پیامک انبوه.

### ۳. از دو بخش لاگ، یکی همیشه خالی بود

هر دو بخش در منو بودند: «لاگ سرور» و «تاریخچه فعالیت». مشکل این بود که دومی
**هیچ‌وقت چیزی نشان نمی‌داد**.

`AuditLog.Record` عمداً ذخیره نمی‌کند؛ ردیف را در change tracker می‌گذارد تا با
همان تراکنشِ تغییری که توصیفش می‌کند commit شود. اما چهار سرویس تنظیمات —
پرداخت، ارسال، پیامک، اعلان مرورگر — بعد از این‌که store خودش ذخیره کرده بود
`Record` را صدا می‌زدند. یعنی چیزی باقی نمانده بود که آن ردیف را بنویسد و ردیف
بی‌صدا دور ریخته می‌شد.

هر چهار سرویس حالا `IUnitOfWork` می‌گیرند و بعد از ثبت، ذخیره می‌کنند. روی استک
واقعی: پیش از اصلاح، تغییر تنظیمات درگاه **صفر** ردیف می‌نوشت؛ بعد از آن،
`payment.settings.saved` در جدول است.

### ۴. گزارش‌ها خلاصه بودند و PDF نداشتند

هر خروجی، **خلاصه‌ی داشبورد** را صادر می‌کرد: «فروش» شش عدد جمع روزانه بود و
«مشتریان» یک ردیف جمع کل برای تمام فروشگاه. این شکلِ درست برای نمودار است و
برای گزارش بی‌فایده — «چه کالایی، به چه کسی، چه تاریخی، به چه قیمتی، پولش رسید
یا نه» از هیچ فایلی که پنل تولید می‌کرد قابل جواب نبود.

شش نوع ردیف تفصیلی اضافه شد، هرکدام یک سطر به ازای هر چیزی که واقعاً اتفاق
افتاده: فروش (یک سطر برای هر قلم هر سفارش)، سفارش‌ها، مشتریان، موجودی (به ازای
هر SKU، چون موجودی روی SKU است و گزارش سطح‌محصول نمی‌گوید کدام سایز تمام شده)،
مالی، و کمپین‌ها. تاریخ‌ها شمسی و به وقت تهران‌اند، وضعیت‌ها فارسی، و عنوان
ستون‌ها از `[ReportColumn]` می‌آید نه از نام property — گزارشی که برای مغازه‌دار
است نباید ستونی به اسم `UnitPrice` داشته باشد.

**PDF** هم ساخته شد. دلیل قبلیِ نداشتنش واقعی بود: PDF فونت با خود حمل نمی‌کند و
نویسنده‌ای که نه فونت جاسازی کند و نه راست‌چین بچیند، صفحه‌ای از حروف جدا و
برعکس تولید می‌کند که از نبودِ فایل بدتر است. حالا هر دو انجام می‌شود —
QuestPDF با Vazirmatn که به‌صورت embedded resource داخل اسمبلی است، نه از روی
میزبان (ایمیج کانتینر هیچ فونت فارسی ندارد). `libfontconfig1` به ایمیج API
اضافه شد چون SkiaSharp بدون آن اولین خروجی را با خطا رد می‌کند.

روی استک واقعی هر سه فرمت صف شدند و هر سه `Completed` شدند. PDF باز شد و
بررسی شد: ۴ × ۸۵۰,۰۰۰ = ۳,۴۰۰,۰۰۰ با ترتیب درست ارقام (اگر برعکس بود
۰۰۰,۰۵۸ خوانده می‌شد)، حروف چسبیده، ستون‌ها از راست به چپ.

### ۵. درگاه آزمایشی اصلاً کار نمی‌کرد

دو ایراد پشت سر هم. بدنه‌ی ناقص باعث **۵۰۰** می‌شد (فیلدهای رشته‌ای در record
غیرnullable‌اند ولی روی سیم اختیاری، و اولین `.Trim()` می‌ترکید). بدنه‌ی کامل
هم **۴۰۰** می‌گرفت، چون آدرس بازگشت خالی رد می‌شد — و برای درگاهی که با هیچ
بانکی حرف نمی‌زند، اپراتور چیزی ندارد که در آن فیلد بنویسد. فیلد خالی شروع
می‌شود، پس مسیر عادی «درگاه آزمایشی را انتخاب کن و ذخیره بزن» همیشه شکست
می‌خورد و پیامش هم فقط «ذخیره اطلاعات انجام نشد» بود.

حالا برای sandbox آدرس بازگشت از `Payment:ReturnUrl` پر می‌شود (همان که
docker-compose به فروشگاه واقعی اشاره می‌دهد)، nullها گارد شده‌اند، و پیام‌های رد
نام فیلد را می‌گویند. درگاه‌های واقعی همچنان آدرس می‌خواهند، چون آن آدرس روی
ترمینال ثبت شده و حدس‌زدنش از رد کردن بدتر است.

### ۶. سبد خرید بعد از خرید خالی نمی‌شد

کد خالی‌کردن سر جایش بود و درست هم بود. مشکل این بود که **سفارش اصلاً ثبت
نمی‌شد**: تنها درگاه در دسترس، درگاه آزمایشی بود و مورد ۵ نمی‌گذاشت انتخاب شود.
سبد خالی نمی‌شد چون خریدی تمام نمی‌شد.

با درگاه اصلاح‌شده، در مرورگر واقعی: کالا به سبد، آدرس، پرداخت اینترنتی، ثبت
سفارش → `BZ-259251-P8P3` با وضعیت پرداخت `Paid`، صفحه‌ی «پرداخت موفق»، و سبد
**صفر** قلم با نشانگر پاک‌شده.

### ۷. محتوای پنل و مجله دو دنیای جدا بودند

صفحه‌ی «محتوا» فقط `content_entries` را فهرست می‌کرد. مقاله‌هایی که مجله واقعاً
سرو می‌کند در `/content/articles` مدیریت می‌شوند و آن صفحه **نه در منو بود و نه
در فهرست** — پس اپراتور هیچ‌وقت پیدایش نمی‌کرد، و هرچه زیر «محتوا» می‌ساخت به
جدولی می‌رفت که مجله نمی‌خواند.

حالا هر دو جدول در یک فهرست‌اند، ردیف مقاله برچسب «در مجله» یا «قدیمی — در مجله
نیست» می‌گیرد، و «مقالات مجله» به منو اضافه شد.

**اما نیمه‌ی دوم مشکل جای دیگری بود.** پس از اصلاح بالا، مقاله‌ی تازه در
`/magazine` ظاهر نمی‌شد. علت: middleware فروشگاه هر درخواست غیرامنِ بدون مبدأ را
رد می‌کند، و فراخوانی پاک‌سازی کش پنل دقیقاً چنین درخواستی است — سرور به سرور،
بدون مرورگر، بدون `Origin`. نگهبان ۴۰۳ می‌داد، `revalidateStorefront` طبق طراحی
خطا را می‌بلعید، و tag هرگز پاک نمی‌شد.

یعنی **هیچ‌کدام** از پاک‌سازی‌های کش کار نمی‌کرد: محصول، دسته، برند، تنظیمات،
باشگاه — همه. `/api/revalidate` از نگهبان مستثنا شد (نه با شل‌کردن خودِ نگهبان):
آن مسیر با `API_KEY` احراز هویت می‌کند، کلید از شبکه‌ی compose بیرون نمی‌رود، و
خودش قبل از دست‌زدن به هر tag آن را بررسی می‌کند. تست شد: با کلید ۲۰۰ و tag پاک
می‌شود، بی‌کلید ۴۰۱. بعد از آن، مقاله‌ی منتشرشده در پنل **بلافاصله** در فهرست
مجله دیده شد.
